# 开题答辩：常见问题与回答策略

> 每题附"回答思路"（怎么组织语言）+ "应答范例"（可以直接用）。

---

## 一、创新点与方法论

### Q1：你的核心创新点到底是什么？

**回答思路**：不要泛泛说"我改进了 GAN"，直接落到**损失函数**上——这是最具体、最不容易被反驳的。

**应答范例**：
> 核心创新在生成器的损失函数设计上。传统 WGAN-GP 只有一项对抗损失，生成器是盲生成——复现真实分布，但不关心生成的样本对下游分类器有没有训练价值。我在对抗损失之外引入了两项新损失：一是边界感知损失 L_boundary，来自 Phase 2 知识蒸馏后的 NN 代理，生成样本偏离决策边界就会受罚，把生成器推向分类器最拿不准的区域；二是违约倾向一致性损失 L_r，来自 Phase 1，约束生成样本的违约速度模式和输入条件 r 匹配。预实验验证了两点：边界富集度从 33.2% 提升到 59.2%，三项损失同步收敛不冲突。

---

### Q2：边界损失的思路不新鲜，信息量大的样本加权训练早就有了，你新在哪？

**回答思路**：区别在于**样本加权是挑已有的，边界损失是生成新的**。

**应答范例**：
> 传统的样本加权或 hard example mining 是在已有数据里挑——挑来挑去就那些样本，少数类的信息量天花板在数据量。边界损失的作用是让生成器**创造新的边界样本**，突破了原有数据的信息量上限。而且边界损失是可微的——通过知识蒸馏把 XGBoost 的边界判断能力复制到 NN 里，嵌进 GAN 的梯度链做端到端优化，这不是简单的样本加权能做到的。

---

### Q3：你这两个损失函数，哪个贡献更大？

**回答思路**：不要硬说谁大，讲清楚各自不可替代的作用 + 用预实验数据支撑。

**应答范例**：
> 边界富集度 59.2% vs 33.2% 说明 L_boundary 的效果非常显著。但这两个损失解决的是不同维度的问题——L_boundary 管"在哪生成"，L_r 管"生成什么模式"，互相不可替代。正式实验会通过消融实验分别量化：A（纯 WGAN-GP）→ B（仅条件）→ C（仅边界）→ D（条件+边界）→ E（完整），每一步的增量贡献都会量化。

---

### Q4：为什么选 WGAN-GP，而不是其他 GAN 变体（如 CTGAN、TabDDPM）？

**回答思路**：WGAN-GP 是表格数据生成领域最成熟稳定的基线，引用文献支撑。

**应答范例**：
> WGAN-GP 用 Wasserstein 距离替代 JS 散度，配合梯度惩罚，解决了原始 GAN 训练不稳定和模式崩溃的问题——在表格数据生成领域是目前使用最广泛、最稳定的 GAN 变体。CTGAN 主要面向混合类型数据，TabDDPM 是扩散模型路线，计算成本更高。我的方法核心贡献在损失函数设计上，选 WGAN-GP 作为基础架构是合理的——而且消融实验的基线 A 就是纯 WGAN-GP，对比是公平的。

---

### Q5：知识蒸馏在你的方法里是核心贡献吗？

**回答思路**：不是，是**使能技术**——没有它创新点跑不通，但它本身不是创新。

**应答范例**：
> 知识蒸馏不是我的创新点，它是实现边界损失和 r 一致性损失的**必要条件**。XGBoost 在表格数据上效果好，但它是树模型，不可微，梯度传不回生成器。蒸馏到 NN 是为了让 L_boundary 和 L_r 嵌进 PyTorch 的梯度链。Phase 2 的蒸馏效果非常好，r=0.983，一致性 94.2%，说明这个方案可行。Phase 1 的蒸馏 r=0.822，够用但还有提升空间。

---

## 二、预实验与可行性

### Q6：预实验 AUC 提升只有 +0.0015，方法真的有效吗？

**回答思路**：这是最关键的问题，必须正面回应。承认数值小，用三个证据兜底，指出限制原因和改进方案。

**应答范例**：
> AUC 提升确实很小，这个我坦诚。但预实验的目标不是拿显著的 AUC 提升，而是验证技术可行性——三个关键证据：
> 1. 边界富集度从 33.2% 提升到 59.2%，证明边界损失确实改变了生成器的行为，方向正确
> 2. 三项损失同步稳定收敛，互不冲突，证明多损失联合优化可行
> 3. Brier 分数从 0.20 降到 0.06，生成样本改善了概率校准
>
> AUC 没拉开的原因是基分类器 AUC 只有 0.735，边界太模糊太宽——37% 的样本在 d<0.2 区域，边界聚焦优势被稀释了。正式实验会增强基分类器到 0.80+，加长 GAN 训练到 500+ epoch，预期边界变锐利后富集度优势会转化为显著的 AUC 增益。

---

### Q7：为什么基分类器 AUC 只有 0.735？是不是数据或者特征有问题？

**回答思路**：承认限制，指出原因，给出改进方案。

**应答范例**：
> 两个原因。一是预实验用了 1:5 欠采样训练基分类器，损失了大量非违约样本的信息。二是特征目前只有 13 维 WOE 编码，信息压缩过程中有损失。改进方案已经明确了：正式实验用全量数据 + scale_pos_weight 训基分类器，预期 AUC 提升到 0.80 以上。

---

### Q8：预实验才跑了 200 epoch，够吗？

**回答思路**：预实验够（验证可行性），正式实验不够。

**应答范例**：
> 预实验的目的是验证"能不能跑通"，200 epoch 足够看到三项损失各自收敛、边界富集度产生显著差异。正式实验会长很多——500 到 2000 epoch，看收敛曲线决定。而且预实验是 CPU 跑的，正式实验会考虑 GPU，时间成本可控。

---

## 三、数据与实验设计

### Q9：你的 OOT 测试集为什么不含合成样本？

**回答思路**：这是核心设计约束，讲清楚否则全篇结论作废。

**应答范例**：
> 如果合成样本混入 OOT，那就不是"跨周期泛化"了——合成样本由低利率期数据训练的 GAN 生成，它们天然带着低利率期的分布特征。OOT 测的是模型面对真实的高利率期数据时的表现，混入合成样本会导致 AUC 虚高，跨周期结论无效。这是从实验设计上就严格控制的——OOT 只含 2023Q1-Q3 的真实完整观测数据，一个合成样本都没有。

---

### Q10：OOS 和 OOT 有什么区别？为什么需要两套测试集？

**回答思路**：用最直白的话说清楚——OOS 回答"学会了没"，OOT 回答"换环境还能用吗"。

**应答范例**：
> OOS 是均衡训练集的 20% 留出，和训练集同分布，含合成样本——回答的是"模型在训练域内拟合是否充分"。OOT 是 2023Q1-Q3 的全部真实数据，独立时间段，不含任何合成样本——回答的是"低利率期训出的模型，在加息期还能用吗"。我论文标题里有"跨经济周期"四个字，OOT 上的表现才是核心证据。所有方法在训练同期的 OOS 上都不差，在 OOT 上的 AUC 衰退幅度才是区分方法好坏的指标。

---

### Q11：删失数据只有 8,150 条，够不够？加了真的有用吗？

**回答思路**：数量不是关键，它带来的是"加息周期的特征分布"而非标签。

**应答范例**：
> 删失数据不贡献 speed_label，它贡献的是加息周期初期违约样本的**特征分布**。8,150 条听起来不多，但违约样本总量也就 35,411，它占了 23%。如果丢掉，整个 GAN 训练只见过低利率期的违约特征，跨周期泛化就缺乏信息基础。Phase 1 的 speed_clf 在完整数据上学会"什么特征对应快/慢违约"后，对删失样本做 r 推断，就把加息周期的信号注入了训练。

---

### Q12：你用的 13 维 WOE 特征够吗？为什么不做特征工程？

**回答思路**：WOE 是信贷风控的行业标准做法，但承认有信息损失，正式实验可能扩展。

**应答范例**：
> WOE 编码是信贷评分卡领域的事实标准——把类别变量按违约风险单调映射到连续值，能让线性模型和树模型都稳定工作。但压缩过程中确实有信息损失。预实验先用 13 维验证方法可行性，正式实验会尝试两种方案：一是使用原始 23 维 one-hot 特征；二是 WOE + 原始特征混合。最终哪个方案效果好就用哪个。

---

### Q13：数据跨度只有三年多（2022Q1-2025Q2），真的算跨周期吗？

**回答思路**：抓住"利率拐点"这个关键——2022 低利率 → 2023Q4 加息，构成了宏观环境的结构性变化。

**应答范例**：
> 2022 年到 2023 年底，美联储经历了一轮显著的加息周期。我的训练数据来自 2022Q1 到 2023Q3 的低利率环境，OOT 测试数据来自 2023Q1-Q3——虽然时间上有一点重叠，但 2023 年的贷款已经经历了利率上升的影响。更关键的是右删失数据来自 2023Q4 之后的加息高峰期。训练域和测试域的宏观环境确实不同，"跨周期"的表述是成立的。论文题目用的是"跨经济周期"，答辩时会精确说明是"跨越利率周期的信贷风险预测"。

---

## 四、实用与落地

### Q14：你这个方法的计算成本高吗？实际能用吗？

**回答思路**：承认有一定成本，但给出合理性解释。

**应答范例**：
> 三阶段训练确实比直接跑一个 XGBoost 贵——预实验在 CPU 上 Phase 3 跑了约 7 分钟，200 epoch。但信贷风控场景下，模型是离线训练、在线推理的。训练成本是一次性的，推理时只跑 Stacking 集成模型，不涉及 GAN。而且生成器训好后就固定了，生成合成样本也是一次性的。对于一个企业级的信贷风控系统来说，这个成本是完全可以接受的。

---

### Q15：你的方法只适用于信贷场景吗？

**回答思路**：不是，但不展开，避免显得不够聚焦。

**应答范例**：
> 方法是通用的——任何类别不平衡 + 存在决策边界模糊区域 + 需要定向生成少数类样本的场景都适用，比如欺诈检测、罕见病诊断。但我的论文聚焦在信贷风险预测上，数据集、特征工程、评估指标都是围绕这个场景设计的。方法的通用性可以在未来工作中讨论。

---

### Q16：如果最终实验结果不显著，Plan B 是什么？

**回答思路**：有备选方案，表现出学术成熟度。

**应答范例**：
> 预实验已经验证了方法的技术可行性，但学术研究不能赌结果。如果消融实验显示某项改进确实没有效果，那就如实报告——"边界损失在弱基分类器下效果有限"，这本身也是有价值的发现。如果整体效果不理想，Plan B 是退回到 cWGAN-GP 方案（仅条件生成，不上边界损失），或者转向研究"为什么边界聚焦在信贷场景下效果不如预期"，这也可以是一篇合格的硕士论文。

---

## 五、理论基础

### Q17：为什么要用 XGBoost 做 Stacking 的最终分类器？和 GAN 里的 XGBoost 是什么关系？

**回答思路**：区分三个 XGBoost 模型的不同角色。

**应答范例**：
> 我的方法里涉及三个 XGBoost：Phase 1 的 speed_clf 判违约快慢，Phase 2 的 base_clf 定位决策边界，Stacking 阶段的最终分类器做违约预测。它们是**独立训练的，各司其职**。Stacking 选 XGBoost 是因为它在表格数据上的性能已经被无数论文和比赛验证过，是这个领域的基准模型。

---

### Q18：为什么不把 GAN 生成器和 Stacking 分类器做端到端联合训练？

**回答思路**：可行但太复杂，硕士论文不做过度工程化。

**应答范例**：
> 端到端联合训练在理论上可以做，但把 GAN 生成、知识蒸馏、Stacking 集成塞进一个训练循环会导致训练极不稳定，收敛困难。分阶段训练更可控，每个环节的效果可以独立评估和调试。而且我的核心创新在 GAN 的损失函数设计，不是训练架构，分阶段训练已经足够验证这个创新点了。

---

## 六、生成器训练与模型细节

### Q19：生成器的完整损失函数是什么？三项损失分别怎么算？

**应答范例**：
> 判别器损失和标准 WGAN-GP 完全相同：
> `L_D = E[D(x̃)] − E[D(x)] + 10.0 × GP`
>
> 生成器损失是我的创新，三项联合优化：
> `L_G = L_adv + 0.1 × L_boundary + 0.05 × L_r`
>
> L_adv = −E[D(G(z, r))]：对抗损失，让生成样本骗过判别器。
>
> L_boundary = BCE(NN_boundary(G(z, r)), 0.5)：边界感知损失，生成样本送进 Phase 2 蒸馏来的 NN_boundary，用二元交叉熵逼输出 p → 0.5。p 偏离 0.5 越远，交叉熵越大，惩罚越重。
>
> L_r = |NN_r(G(z, r)) − r|：违约倾向一致性损失，生成样本送进 NN_r 反推 r̂，和当初输入生成器的 r 做绝对值偏差。逼生成器按 r 指令干活。

---

### Q20：三个损失的权重 λ_boundary=0.1、λ_r=0.05、λ_gp=10.0 怎么定的？

**应答范例**：
> λ_gp=10.0 是 WGAN-GP 原论文在多个数据集上做消融后确定的，之后几乎所有 WGAN-GP 工作都用这个值，已经是社区默认配置，不需要自己论证。
>
> λ_boundary=0.1 和 λ_r=0.05 是预实验手动设定的，遵循三个原则：一是辅助损失不能盖过主任务，所以 λ 都远小于 1；二是边界损失是核心创新，权重要大于一致性损失，所以 2:1；三是量纲对齐——L_boundary 和 L_r 都是交叉熵，约 0.6–0.7 的量级，乘 0.1 后约 0.06–0.07，占 L_adv（≈1.0）的 5%–7%，刚好够推动生成器调整行为又不喧宾夺主。
>
> 预实验的收敛曲线验证了这个配置合理——三项损失无冲突。正式实验会在 0.01–0.5 范围内做单变量敏感性分析，系统确定最优权重。

---

### Q21：GAN 训练中 D 和 G 怎么交替更新？谁先动？

**应答范例**：
> D 先动。一个训练批次的标准流程是：冻结 G，更新 D 5 次（n_critic=5）→ 冻结 D，更新 G 1 次。D 多更新几次是为了让它先学到"真假之分"，再给 G 提供有意义的对抗信号——这是 WGAN-GP 原论文的标准设定。
>
> 交替冻结的意思是：D 更新时 G 照样生成假样本、照样算 loss，但梯度只传到 D，不进入 G 的参数；G 更新时 D 照样打分，但 D 的参数不动，梯度穿过 D 传回 G。一轮接一轮，双方交替升级——第 t 轮 D 更新产出 D(t)，立刻被 G 拿来用于自己的更新，产出 G(t)；第 t+1 轮 D 用 G(t) 生成的假样本更新，产出 D(t+1)，以此类推，直到收敛。
>
> 优化器都是 Adam，学习率 1e-4。

---

### Q22：Adam 优化器是什么？为什么用 Adam 不用 SGD？

**应答范例**：
> SGD 是最原始的梯度下降——每一步固定步长往梯度方向走。问题在于坡度陡的地方容易冲过头，坡度平的地方寸步难行。
>
> Adam 同时做了两件事：一是惯性（动量项），记住过去的梯度方向，保持前进稳定不被局部小坑困住；二是自适应步长，每个参数各管各的步长——变化剧烈的参数步长自动变小求稳，变化平缓的参数步长自动变大求快。
>
> GAN 训练天然不稳定，G 和 D 交替对抗，损失曲面大幅震荡。Adam 的惯性项能压住震荡不让训练崩，自适应步长让每个参数用适合自己的速度前进。WGAN-GP 原论文也用的 Adam，学习率 1e-4。

---

### Q23：z 和 r 分别是什么？r 是固定的还是变化的？

**应答范例**：
> z 是 100 维随机噪声，每次生成时从标准正态分布 N(0,1) 现场抽。它提供随机性——同一个 r 配合不同 z，能生成违约速度模式相近但具体特征值不同的样本。
>
> r 是违约速度倾向得分，1 维，来自 Phase 1 的 speed_clf，取值范围 [0,1]。r→1 为快违约，r→0 为慢违约，提供条件指令。
>
> r 对单笔真实违约样本是固定的（Phase 1 算好存下来不变），但每批训练随机抽不同样本，r 值一直在变——从 0.08 到 0.95 都有。正是这种变化逼着生成器学会"不同 r 对应不同违约特征模式"。如果 r 全一样，条件生成就失去意义了。

---

### Q24：为什么不直接用真实违约样本当生成器的输入，而是用随机噪声 z？

**应答范例**：
> 生成器的任务是创造新的违约样本，不是复制已有样本。如果用真实数据当输入，生成器学到的最优策略就是原样输出——进去什么出来什么，变成自编码器了，一个样本都创造不出来。
>
> z 是纯随机噪声，不带任何真实数据信息。生成器必须从噪声里凭空构建出 13 维特征，这个过程中它在学习真实违约数据的底层分布规律。学会了之后，随便抽一组 z + r，就能生成"像真的但数据里没有"的新样本。
>
> 真实违约数据也没有浪费——它们训练判别器、算 L_boundary 和 L_r，是 G 努力模仿的"标准答案"，只是不作为输入。

---

### Q25：蒸馏用的是什么损失函数？为什么？

**应答范例**：
> BCELoss（二元交叉熵）。Phase 2 的训练代码就是 `loss_fn = nn.BCELoss(); loss = loss_fn(NN(x), p_xgb)`，NN 的输出去逼近 XGBoost 输出的概率值。
>
> MSE 也可以用，但 BCELoss 在概率输出场景下梯度形状更好——当 NN 输出和 XGBoost 输出差得远时，BCE 给的梯度更猛，收敛更快。和普通分类训练的区别是：普通训练学的是硬标签 0/1，蒸馏学的是 XGBoost 输出的软概率 0.48/0.73……NN 不是学对错，是学 XGBoost 的判断习惯。这就是蒸馏的本质。

---

### Q26：蒸馏成功的标准是什么？为什么 Phase 1 的 r>0.8 就算有效？

**应答范例**：
> 看两个指标：Pearson r（趋势是否一致）和 MAE（单点精度）。Phase 1 的 NN_r 达到 r=0.822，MAE=0.031。
>
> NN 代理的使命不是百分之百复刻 XGBoost 的输出值，而是在 GAN 训练中提供正确的梯度方向。梯度不关心绝对值差多少，关心的是"往哪个方向调"。r>0.8 意味着决定系数 r²≈0.68——XGBoost 输出中约 68% 的变异能被 NN 捕捉到。对大方向信号来说足够了——生成器收到"倾向更高 r"和"倾向更低 r"的指令时，不会走反。
>
> 0.8 这个阈值不是数学推导出来的严格门限，是统计学中相关系数"强"这个档的常规分界（0.7–0.9），也是同类蒸馏工作的常见实践标准。Phase 2 的 r=0.983 远超这个门槛，因为判"违不违约"的信号远比判"快还是慢"强。

---

### Q27：为什么非要用蒸馏？直接用 NN 做基分类器不行吗？

**应答范例**：
> 预实验 v1 已经试过直接用 NN 做基分类器——AUC 只有 0.735，画的边界又糊又宽。不是 NN 没调好，而是表格数据上 NN 天然打不过 XGBoost。表格数据的特征是相互独立的——信用分和负债率之间没有像素那种空间关系，没有词那种序列关系，NN 的卷积和注意力机制全用不上，只剩全连接层做矩阵乘法。XGBoost 树结构逐列分裂，天然适合这种场景。
>
> 因果关系是反过来的——XGBoost 本来就是边界判断的最优选择。但它不可微，梯度传不回生成器。蒸馏让 XGBoost 当老师、NN 当学生，用 BCELoss 让 NN 模仿 XGBoost 的输出习惯。蒸馏后 NN 复刻度 r=0.983，边界一致性 94.2%，几乎完美传承了老师的判断能力——但关键是 NN 处处可微，能嵌进 GAN 的梯度链。
>
> 所以不是"XGBoost 帮 NN 训练"，而是"NN 帮 XGBoost 传梯度"。蒸馏是桥，连上了"最好的边界判断"和"必须求导的训练框架"这两端。

---

### Q28：为什么要看边界富集度和 Brier 分数？分别怎么定义？

**应答范例**：
> **边界富集度**验证核心创新——边界损失是否真的改变了生成器行为。定义：生成样本中 d<0.2（即 p∈[0.3, 0.7]）的占比。33.2%（盲生成）→59.2%（加边界损失），提升 1.8 倍，这是行为层面的直接证据。没有这个指标，你不知道生成器到底往没往边界走，创新点在实证上是空的。
>
> **Brier 分数**验证生成样本的实用价值——改善了概率校准。定义：Brier=(1/n)×Σ(p_i−y_i)²，预测概率和真实结果的均方误差，越小越好。从不平衡数据的 0.20 降到 0.06，说明加了边界样本后分类器不再"全给低分糊弄事"，输出的概率值开始靠谱了。
>
> AUC 和 Brier 互补——AUC 高但 Brier 烂，排序对但概率离谱，业务上用不了；Brier 好但 AUC 没涨，概率准了但排序能力没突破（预实验现状）。两个指标分别回答"方法是否生效"和"样本是否有用"。

---

### Q29：什么是梯度？反向传播是怎么工作的？

**应答范例**：
> 梯度可以理解为一个箭头，告诉你"往哪个方向改能更好"。G 生成了一批样本，NN_boundary 看完说 p=0.92（离边界远，损失大），梯度算出来告诉 G："把第 37 号参数调低 0.003，第 128 号参数调高 0.001……"，G 照做，下次生成的样本 p=0.58——离边界近了，损失小了。
>
> 在你的 GAN 里，G 每生成一个样本，三条梯度分别从 D、NN_boundary、NN_r 的输出端往回走，穿过各自的网络层，最终在 G 的最后一层汇合、加在一起，更新 G 的参数。D、NN_boundary、NN_r 三个评分器都站在 G 的出口，各自给出反馈——D 说不够真，NN_boundary 说没在边界，NN_r 说违约速度不对。三路梯度合在一起推着 G 往三个目标同时优化。

---

### Q30：观测窗口为什么选 24 个月？中位数 12 个月是什么？

**应答范例**：
> 24 个月是抵押贷款违约预测的学术惯例——FM 数据研究的常见基准设定。抵押贷款违约有滞后性，借款人通常扛几个月到一年后才断供，24 个月能覆盖绝大多数违约的暴露期，同时保证有足够的完整观测样本。如果窗口太短（如 12 个月），会漏掉大量慢违约样本；太长（如 36 个月），最新一批贷款（2023Q3 放款）的观测就不完整，可用样本急剧缩水。
>
> 12 个月是中位数——全部完整违约样本的违约时间取中位数，一半在 12 个月内违约，一半在 12 个月后。把它作为 Early/Late Default 的切分阈值，保住了两类的样本量大致均衡（约 11,000 vs 16,000），训练 speed_clf 不会偏。

### Q31：base_clf 和后面 Stacking 分类器是什么关系？边界是谁的边界？

**应答范例**：
> 边界是 base_clf 的决策边界，不是 Stacking 模型的。base_clf 是"工具人"——它只在 Phase 2/3 服务，画出边界让 GAN 知道往哪生成，训完用完就可以退场。Stacking 模型是"运动员"——均衡训练集备好后上场训练，去 OOS/OOT 做预测出结果。论文最终要评估的是 Stacking 的 AUC，不是 base_clf 的。
>
> GAN 不直接做预测。整条链是：base_clf 画边界 → GAN 往边界生成合成样本 → 合成样本混入训练集 → 喂给 Stacking 训练 → Stacking 上场考试。GAN 是数据增强引擎，Stacking 是最终预测器。

---

### Q32：传统边界约束为什么无法嵌入 GAN 训练？技术痛点在哪？

**应答范例**：
> GAN 训练靠梯度反向传播。你想让生成器往边界生成，需要一个评分器判断"样本在不在边界附近"，然后把这个判断转化成梯度传回生成器。
>
> XGBoost 做边界判断最强——但它不可微。判断过程是几百棵树的分叉投票，结果是离散的，梯度在 XGBoost 这一步就断了。L_boundary 算得出数值，但传不回去，等于形同虚设。这就是传统手段（树模型做边界判断）和深度学习训练框架（必须求导）之间的根本矛盾。
>
> 我的方案是知识蒸馏——把 XGBoost 的边界判断能力照搬到一个 NN 里。NN 全是矩阵运算，每一层可导，塞进 PyTorch 梯度链就能顺畅回传。蒸馏就是给 XGBoost 配一个能说"微分语言"的翻译。

---

### Q33："生成的样本对下游分类器有训练价值"是什么意思？

**回答思路**：用"一眼判对"vs"让人纠结"的对比说明。

**应答范例**：
> 分类器对 p=0.97 的样本（铁定违约）和 p=0.03 的样本（铁定不违约）判得非常笃定——这类样本看再多也学不到新东西。真正有用的是 p≈0.5 的边界样本——分类器对它们非常纠结，看了之后被逼着学更精细的判断规则。
>
> BA-cWGAN-GP 生成的就是后一类——不是在随便生成"像违约的"，而是专门生成分类器看了会困惑、会进步的那种违约样本。边界富集度 59.2% 就是在量化这件事：生成器产出的样本里，近六成都在分类器最需要学习的区域。

---

## 七、答辩策略总纲

| 原则 | 说明 |
|------|------|
| **承认局限，给出方案** | 被问到 AUC 低、epoch 少、特征维度低——不辩解，直接说"这是预实验的限制，正式实验已规划改进方案" |
| **用数字说话** | 被问到效果——用 59.2% vs 33.2%、r=0.983、L_r=0.079 这些具体数据，不要去描述"很好""不错" |
| **创新点落到位** | 反复回到**损失函数**这个具体锚点上，避免泛泛说"我改进了 GAN" |
| **不硬刚评审** | 如果评审坚持某个观点，先认可合理部分再补充解释，不要进入对抗模式 |
| **预实验定位清晰** | 始终强调预实验是"可行性验证"不是"最终性能"，避免被用正式实验标准来要求 |
