# BA-cWGAN-GP 算法详解

> 本文档从零开始，串讲 BA-cWGAN-GP 算法的完整框架与实现细节。适合作为开题答辩前的复习材料。

---

## 目录

1. [问题背景：为什么要搞这么复杂](#一问题背景为什么要搞这么复杂)
2. [整体框架：三个 Phase 怎么分工](#二整体框架三个-phase-怎么分工)
3. [Phase 1：违约速度分类器（speed_clf）—— 为生成器装上"方向盘"](#三phase-1违约速度分类器speed_clf--为生成器装上方向盘)
4. [Phase 2：基分类器（base_clf）—— 为生成器装上"导航系统"](#四phase-2基分类器base_clf--为生成器装上导航系统)
5. [知识蒸馏：为什么 XGBoost 不够，还要训 NN](#五知识蒸馏为什么-xgboost-不够还要训-nn)
6. [Phase 3：BA-cWGAN-GP 训练 —— 三合一生成](#六phase-3ba-cwgan-gp-训练--三合一生成)
7. [删失数据：无标签，但有大用](#七删失数据无标签但有大用)
8. [与传统 WGAN-GP 的对比](#八与传统-wgan-gp-的对比)
9. [预实验如何验证两个创新点](#九预实验如何验证两个创新点)

---

## 一、问题背景：为什么要搞这么复杂

你的论文要解决的核心问题：**信贷违约预测中，违约样本极少（约 4%），类别严重不平衡，传统分类器效果受限。**

直觉上的解决方案：用 GAN 生成一批逼真的违约样本，补充进去，平衡训练集。

但普通 GAN（比如 WGAN-GP）有一个问题——它是**盲生成**：

```
噪声 z → 生成器 → "像违约"的样本
```

生成器只关心一件事：生成的样本是不是足够"像真的"，能骗过判别器。至于这个样本**对下游分类器有没有训练价值**，它完全不考虑。

打个比方：一个教练想训练拳击手（下游分类器），找了一堆陪练（GAN 生成的样本）。盲生成相当于随便在街上拉人当陪练——确实是人，但水平参差不齐，大多数一拳就倒，拳击手练不出东西。

你的方法要做的是：**生成器不能随便生成，每条生成样本都必须对下游分类器有最大训练价值。** 具体来说就是两个方向：

1. **条件约束（c）**：生成样本的违约速度模式要可控——能按需生成快违约或慢违约
2. **边界约束（BA）**：生成位置要精准——集中在分类器最拿不准的决策边界附近

这个"能感知边界、按条件定向生成"的 GAN，就是 BA-cWGAN-GP。

---

## 二、整体框架：三个 Phase 怎么分工

```
┌─────────────────────────────────────────────────────────────────────┐
│                        BA-cWGAN-GP 算法框架                          │
├──────────────┬──────────────────┬────────────────────────────────────┤
│   Phase 1    │     Phase 2      │            Phase 3                │
│  违约速度提取 │   决策边界识别    │         BA-cWGAN-GP 训练           │
├──────────────┼──────────────────┼────────────────────────────────────┤
│              │                  │                                    │
│  speed_clf   │   base_clf       │  输入: z(100维) + r(1维)           │
│  (XGBoost)   │   (XGBoost)      │        ↓                          │
│      ↓       │       ↓          │  加权采样: w = 1/(d+ε)             │
│   r 值       │   d = |p-0.5|   │        ↓                          │
│      ↓       │       ↓          │  L = L_adv + L_boundary + L_r     │
│  NN_r 代理   │  NN_boundary 代理 │        ↓                          │
│      ↓       │       ↓          │  定向生成 → 边界附近合成违约样本    │
│  提供 L_r    │  提供 L_boundary │                                    │
│              │                  │                                    │
├──────────────┴──────────────────┴────────────────────────────────────┤
│  方法名拆解：                                                        │
│  BA  = Boundary-Aware（边界感知）          ← Phase 2 提供            │
│  c   = conditional（条件生成）             ← Phase 1 提供            │
│  WGAN-GP = Wasserstein GAN + 梯度惩罚      ← 保证训练稳定不崩         │
└─────────────────────────────────────────────────────────────────────┘
```

三个 Phase 的递进关系：

- **Phase 1**：给每笔违约贷款算出一个 r 值（违约快慢倾向），为生成器提供"条件方向盘"
- **Phase 2**：训一个分类器，定位决策边界在哪，为生成器提供"边界导航"
- **Phase 3**：把 r 和边界信息同时注入生成器，三项损失联合优化

下面逐个展开。

---

## 三、Phase 1：违约速度分类器（speed_clf）—— 为生成器装上"方向盘"

### 3.1 speed_clf 是什么

`speed_clf` = speed classifier，一个 XGBoost 二分类模型。

但它**不判"会不会违约"**，它判的是**"既然已经违约了，是快违约还是慢违约"**。

### 3.2 训练数据与标签构造

你有 27,261 笔完整观测的违约贷款，每笔都知道从贷款发放到首次违约用了几个月。用中位数（12 个月）一刀切：

- **≤ 12 个月 → Early Default（快违约）**，标签 = 1
- **> 12 个月 → Late Default（慢违约）**，标签 = 0

输入：13 维 WOE 编码特征（和后续模型用同一套特征）

输出：**r ∈ [0,1]**——这笔贷款属于 Early Default 的概率

- r → 1：特征组合和"放款后很快就违约"的贷款高度相似（撑不久型）
- r → 0：特征组合和"撑了很久才违约"的贷款高度相似（慢慢拖垮型）

### 3.3 为什么区分快违约和慢违约

这两种违约的风险本质不同：

- **快违约**：贷款发放时就埋了雷——借款人资质本身有问题（收入造假、过度负债）
- **慢违约**：借款人本来还行，后来宏观环境恶化（加息、失业）拖垮的

区分这两种模式，下游分类器能学到更细粒度的风险特征。

### 3.4 删失数据的 r 推断

另有 8,150 笔来自 2023Q4+ 的删失违约——它们刚放款不久就违约了，观测窗口被截断，无法判定实际是快还是慢，因此没有 speed_label。

但 speed_clf 训好后是一个**特征 → r 的映射函数**：

```
输入 13 维特征 → 输出 r
```

它推断删失样本时**不需要标签**。删失样本虽然不知道"实际几个月违约"，但有完整的 13 维特征。speed_clf 看到特征组合就能给出 r 判断——比如信用分低、负债率高，"这组合一看就撑不了多久" → 推断 r = 0.87。

最终，全部 35,411 笔违约（27,261 + 8,150）+ 额外的非违约样本，每条都带上了 r 值。

---

## 四、Phase 2：基分类器（base_clf）—— 为生成器装上"导航系统"

### 4.1 base_clf 是什么

`base_clf` = base classifier，一个 XGBoost 分类器。

它判的是**"会不会违约"**（和 speed_clf 判的不是一个东西）：

| | speed_clf（Phase 1）| base_clf（Phase 2）|
|---|---|---|
| 判什么 | 违约**快还是慢** | **违不违约** |
| 输出 | r（违约速度倾向）| p（违约概率）|
| 训练数据 | 27,261 条有标签违约 | 完整 GAN 训练集约 82 万条（含删失）|
| 用途 | 为 GAN 提供条件输入 r | 为 GAN 提供边界信息 |

### 4.2 什么是决策边界

base_clf 训好后，面对任何一笔贷款特征，输出一个概率 p：

- **p = 0.9** → 分类器非常确定会违约（安全区深处）
- **p = 0.1** → 分类器非常确定不会违约（安全区深处）
- **p ≈ 0.5** → 分类器完全拿不准，正在决策边界上反复横跳

想象分类器在特征空间里画了一条线，线的一边判"会违约"，另一边判"不会违约"。**离这条线越近的样本，就是越靠近决策边界的样本。**

### 4.3 为什么边界附近的样本最值钱

回到画线这个比喻。分类器线画在哪，是由**线附近的样本**决定的。安全区深处再多 100 个样本，线几乎不动；边界附近多 10 个样本，线可能就挪了。

换句话说：**分类器最不确定的那些样本，就是它最需要学习的样本。** 把边界样本加入训练集重新训练，分类器能更清楚"线到底该画在哪"。

### 4.4 边界距离 d 和采样权重 w

对每一笔真实违约样本，base_clf 输出 p，然后算两个东西：

**边界距离**：`d = |p − 0.5|`

| 贷款 | p | d | 位置 |
|-------|-----|------|------|
| A | 0.03 | 0.47 | 安全区深处（离边界远）|
| B | 0.48 | **0.02** | **贴着边界** ← 值钱 |
| C | 0.97 | 0.47 | 安全区深处（离边界远）|
| D | 0.55 | **0.05** | **贴着边界** ← 值钱 |

**采样权重**：`w = 1 / (d + ε)`

d 越小（越靠近边界）→ w 越大 → 在 GAN 训练时被抽取的概率越高。

这个权重机制让生成器**看到的边界样本远多于远离边界的样本**——生成器自然就学着往边界区域生成。

### 4.5 base_clf 和删失数据的关系

base_clf 训练时用完整 GAN 训练集（含删失数据的 82 万条）。训好后，base_clf 给全部 35,411 笔违约样本打分——删失样本同样能算出 d 和 w，因为它们有完整的 13 维特征。

---

## 五、知识蒸馏：为什么 XGBoost 不够，还要训 NN

### 5.1 什么是知识蒸馏

知识蒸馏 = 让一个小模型去模仿大模型（或不可微模型）的输出习惯，把"老师"的判断能力复制到"学生"身上。

类比：XGBoost 是个经验丰富的老师傅，能精准判断"这笔贷款会违约吗"、"这笔是快还是慢"。但他不会写字——判断标准没法写成可微的数学公式。于是你让一个 NN 徒弟坐在师傅旁边——来一笔贷款，师傅给出判断（p 或 r），徒弟记下"这种特征的贷款师傅说 p=0.48"。几万笔看下来，徒弟学会了师傅的判断逻辑。

### 5.2 为什么要蒸馏：可微分性

GAN 训练用 PyTorch 框架，生成器靠**梯度反向传播**来更新参数。XGBoost 是树模型——它的预测是几百棵树投票出来的，中间过程是不连续的分叉判断，没法求导。

**核心矛盾**：XGBoost 效果好但不能求导，GAN 必须求导才能回传梯度。

解决方案：训一个 NN 去模仿 XGBoost 的输出。NN 本身是层层矩阵乘法 + 激活函数，天生可微，嵌进 PyTorch 就能顺畅回传梯度。蒸馏完后，XGBoost 老师傅退场，NN 徒弟替他在 GAN 训练里上班。

### 5.3 为什么选 NN

在深度学习框架里唯一能嵌入做梯度回传的模型就是神经网络。随机森林、XGBoost、SVM 都不可微。**不是 NN 最好，是只有 NN 能求导。**

### 5.4 两个蒸馏任务及其效果

| | 老师 | 学生 | 蒸馏效果 | 学生用途 |
|---|---|---|---|---|
| Phase 1 | speed_clf (XGBoost) | NN_r | r = **0.822** | 嵌入 GAN 的 L_r 损失 |
| Phase 2 | base_clf (XGBoost) | NN_boundary | r = **0.983** | 嵌入 GAN 的 L_boundary 损失 |

两个 NN 结构完全相同：`13 → 256 → 128 → 1`，三层全连接。

为什么 Phase 2 蒸馏效果好得多？

- base_clf 判的是"违不违约"，信号强，任务简单，老师本身水平高
- speed_clf 判的是"快还是慢"，任务本身就难，老师水平受限，学生自然也受限

### 5.5 "为什么不让 GAN 直接生成标签，跳过蒸馏"

因为 L_boundary 和 L_r 需要的不是标签（0 或 1），而是**连续的、可微分的评分器**。

- **标签**：只有 0/1，像开关。离边界近（p=0.52）和远（p=0.92）在标签层面都是"1"，无法区分——边界损失没法算。
- **评分**：0 到 1 的连续值，像刻度。p=0.52 → d=0.02（靠近边界，罚得轻），p=0.92 → d=0.42（远离边界，罚得重）。这个连续刻度让梯度可以平滑传导。

即使 XGBoost 本身能输出连续概率值，它也不能嵌进 PyTorch 的梯度链——**XGBoost 卡在生成器和判别器之间的梯度回传路径上，梯度到了它就断了。**

蒸馏的动机不是 XGBoost 输出不了概率，而是**没法对 XGBoost 求导**，梯度传不回生成器。

---

## 六、Phase 3：BA-cWGAN-GP 训练 —— 三合一生成

### 6.1 输入结构

与传统 GAN 不同，BA-cWGAN-GP 的生成器接受的不是纯随机噪声：

```
传统 WGAN-GP:  z (100维纯噪声) → 生成器 → 合成样本

BA-cWGAN-GP:  z (100维噪声) + r (1维条件) → 生成器 → 合成样本
               └── 随机 ──┘   └─ Phase 1 提供 ─┘
```

生成器拿到 r=0.9 → "生成一个快违约的样本"；拿到 r=0.3 → "生成一个慢违约的样本"。

### 6.2 加权采样

GAN 训练时，真实违约样本不是均匀随机抽取的，而是按 Phase 2 算出的权重 w = 1/(d+ε) 加权抽取。边界附近的真实样本被反复喂给生成器，让生成器对边界区域"耳濡目染"。

加权采样管的是**训练时生成器被什么样的真实样本"带"**。

### 6.3 三项损失函数

BA-cWGAN-GP 的生成器同时被三个目标驱动：

```
L_total = L_adv + λ_boundary × L_boundary + λ_r × L_r
```

| 损失 | 干什么 | 提供方 | 计算逻辑 |
|------|--------|--------|----------|
| **L_adv**（对抗损失）| "生成的样本要像真的"——骗过判别器 | GAN 自带的判别器 D | 判别器打分，生成器追高分 |
| **L_boundary**（边界感知损失）| "要在边界附近生成"——对下游分类器最有价值 | Phase 2 的 NN_boundary | 生成样本 → NN_boundary 打分 → p 偏离 0.5 就罚 |
| **L_r**（违约倾向一致性损失）| "生成样本的违约速度要和输入 r 匹配" | Phase 1 的 NN_r | 生成样本 → NN_r 反推 r̂ → r̂ 偏离输入 r 就罚 |

### 6.4 三项损失之间的关系

- **只加 L_boundary，不加 L_r**：生成器全往边界堆，但不管自己是快违约还是慢违约，两类模式混在一起——模式崩溃
- **只加 L_r，不加 L_boundary**：生成器按要求生成快/慢违约，但没有方向感，不知道往哪生成对下游分类器最有帮助
- **三个一起加**：L_r 管"违约模式准确"，L_boundary 管"生成位置有价值"，L_adv 管"整体像真的"——三个方向互相配合

### 6.5 预实验训练结果

| 损失 | 最终值 | 含义 |
|------|--------|------|
| L_adv | -0.98 | 对抗损失收敛，生成样本逼真 |
| L_boundary | -0.17 | 稳定维持，生成器被持续推向边界 |
| L_r | 0.079 | 条件偏差仅约 8%，违约速度模式匹配良好 |

三项损失同步稳定收敛、互不冲突、无梯度爆炸与模式坍塌。

---

## 七、删失数据：无标签，但有大用

### 7.1 删失数据是什么

来自 2023Q4 之后的 8,150 笔违约贷款。观测窗口被截断，无法算出实际违约用了几个月，因此没有 speed_label。

### 7.2 删失数据为什么重要

它们来自**加息周期初期**，携带了加息环境下违约的特征分布。如果丢掉，整个方法链的信息来源就只有低利率期的 27,261 笔违约——生成器永远没见过加息环境下的违约长什么样，跨周期泛化无从谈起。

### 7.3 删失数据的贡献路径

```
27,261 完整违约（有 speed_label）  →  训练 speed_clf
                                            ↓
8,150 删失违约（无 speed_label）     →  speed_clf 推断 r 值
                                            ↓
全部 35,411 违约 + 推断 r            →  送入 Phase 2 base_clf 训练 + Phase 3 GAN 训练
```

删失数据**不贡献标签**，但贡献**加息周期的特征分布**。speed_clf 在低利率期学会"什么特征对应快/慢违约"后，能在删失样本上推断 r 值——两者配合，加息周期的违约信号被注入了训练。

---

## 八、与传统 WGAN-GP 的对比

| 维度 | WGAN-GP | BA-cWGAN-GP |
|------|---------|-------------|
| **输入** | 纯噪声 z (100维) | z (100维) + 条件 r (1维) ← Phase 1 |
| **损失函数** | L_adv 单项 | L_adv + L_boundary + L_r 三项 |
| **采样策略** | 均匀随机采样 | 加权采样 w = 1/(d+ε) ← Phase 2 |
| **生成方向** | 盲生成，复现自然分布 | 定向生成，聚焦决策边界 |
| **边界富集度** | 33.2%（≈ 真实分布 36.9%）| **59.2%**（1.8 倍提升）|
| **生成目标** | "像真的就行" | "像真的 + 在边界 + 速度模式对" |

---

## 九、预实验如何验证两个创新点

两个创新点各有其验证链：

### 9.1 L_boundary 验证链

```
Phase 3 B_loss 稳定在 -0.17     →  证明损失函数本身没崩，梯度正常传导
Phase 4 边界富集度 59.2% vs 33.2% →  证明损失实际改变了生成器行为，生成器真的往边界走了
```

两个指标缺一不可。B_loss 收敛只说明"惩罚在运转"，边界富集度才说明"惩罚产生了效果"。

### 9.2 L_r 验证链

```
Phase 3 R_loss 收敛到 0.079  →  条件一致性约束有效，平均偏差仅 8%
Phase 4 当前预实验未直接量化  →  正式实验可补 r 匹配散点图（r̂ vs r + 相关系数）
```

### 9.3 隐含验证

三项损失同时收敛且不冲突，证明 L_boundary 和 L_r 可以与 L_adv 和平共存——这个在设计时不是理所当然的，预实验证明了多目标联合优化的可行性。

---

## 附录：关键术语速查

| 术语 | 含义 |
|------|------|
| speed_clf | Phase 1 的 XGBoost 违约速度分类器，判"快违约还是慢违约"，输出 r |
| base_clf | Phase 2 的 XGBoost 基分类器，判"违不违约"，输出 p |
| r | 违约速度倾向得分，r→1 为快违约，r→0 为慢违约 |
| p | 违约概率，base_clf 输出，p≈0.5 说明在决策边界上 |
| d | 边界距离，d = \|p − 0.5\|，d 越小离边界越近 |
| w | 采样权重，w = 1/(d+ε)，d 越小权重越大 |
| NN_r | Phase 1 的知识蒸馏产物，模仿 speed_clf，可微分 |
| NN_boundary | Phase 2 的知识蒸馏产物，模仿 base_clf，可微分 |
| L_adv | 对抗损失，让生成样本"像真的" |
| L_boundary | 边界感知损失，让生成样本"在边界附近" |
| L_r | 违约倾向一致性损失，让生成样本的"违约速度匹配输入 r" |
| 删失数据 | 来自 2023Q4+ 的 8,150 笔无 speed_label 的违约 |
| 边界富集度 | 生成样本中 d < 0.2（即 p ∈ [0.3, 0.7]）的占比 |
| 知识蒸馏 | XGBoost（不可微）→ NN（可微）的映射关系复制 |
| WGAN-GP | Wasserstein GAN + Gradient Penalty，训练稳定，不崩不塌 |
