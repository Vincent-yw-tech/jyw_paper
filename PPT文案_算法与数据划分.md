# PPT 单页文案：BA-cWGAN-GP 算法框架与跨周期数据划分

---

## 建议排版结构（一页两栏）

```
┌──────────────────────────────────────────────────────────────┐
│  BA-cWGAN-GP 算法框架与跨周期数据划分策略                       │
├────────────────────────┬─────────────────────────────────────┤
│      左栏：算法框架      │        右栏：数据划分                 │
│       (约 45% 宽度)     │        (约 55% 宽度)                 │
└────────────────────────┴─────────────────────────────────────┘
```

---

## 左栏文案：BA-cWGAN-GP 算法框架

**标题**：三阶段信息提取 → 定向生成

**Phase 1 · 违约倾向得分提取**
- 从月度 Performance 数据回溯首次违约月份，标注 Early / Late Default
- 训练 XGBoost 违约速度分类器（speed_clf），对右删失样本推断 r 值
- 输出：每笔违约贷款的违约倾向得分 **r ∈ [0,1]**

**Phase 2 · 决策边界识别**
- 训练 XGBoost 基分类器 → 计算边界距离 d = |p − 0.5|
- 知识蒸馏：NN 代理模型复刻 XGBoost 边界逻辑（r = 0.983）
- 输出：边界采样权重 **w = 1/(d+ε)**

**Phase 3 · BA-cWGAN-GP 定向生成**
- 条件输入：z（噪声, 100维）+ r（违约倾向, 1维）
- 三项联合损失：
  - L_adv：对抗损失（"以假乱真"）
  - **L_boundary：边界感知损失（"往分类器最拿不准的地方生成"）**
  - L_r：违约倾向一致性（"违约速度模式匹配"）

**核心创新**：生成器不再"盲生成"，而是被边界损失引导至决策边界附近，生成对下游分类器最有训练价值的合成违约样本。

---

## 右栏文案：跨周期数据划分策略

**标题**：一套数据，两个经济周期，三组独立评估

**数据全景**

```
2022Q1 ──────────── 2023Q3 ── 2023Q4 ──── 2025Q2
│                                    │
└─ 完整观测 (~1,015K) ──┘   └─ 右删失违约 (8K) ─┘
     低利率周期（训练域）           加息周期
```

- 第一章 GAN 阶段内部做 80/20 划分（质量自检），输出 **均衡训练集**
- 均衡训练集 = GAN 训练集真实样本 + BA-cWGAN-GP 合成违约样本

**第二章核心划分：三组数据，各司其职**

```
均衡训练集 ──→ ┌── Train (80%) ──→ 5折交叉验证调参，含合成样本
              │
              └── OOS Test (20%) ──→ 同分布测试，含合成样本
                                     答："模型学会了没？"

2023Q1–Q3 ──→ OOT Test (独立全集) ──→ 跨周期测试，不含任何合成样本
完整观测                              答："加息周期还能用吗？" ⭐
```

**OOS vs OOT：为什么需要两套测试集？**

| | OOS（同分布测试） | OOT（跨时期测试） |
|---|---|---|
| 来源 | 与训练集同源（均衡训练集的 20% 留出） | 独立时间段（2023Q1–Q3 全部完整观测） |
| 合成样本 | 含 | **绝对不含** |
| 回答的问题 | 模型拟合是否充分？ | **低利率期训出的模型，在加息期还有效吗？** |
| 如果垮了说明 | 模型容量不够/欠拟合 | 特征-违约的映射关系随宏观环境漂移了 |
| 论文权重 | 辅助指标（可用作对照基线） | **核心证据（跨周期泛化能力）** |

**为什么 OOT 是论文最核心的指标？**

> 论文标题是"**跨经济周期**的信贷风险预测"。如果只在训练同期数据上测——所有方法都不差。真正的考验是：2022 年低利率环境训练的模型，面对 2023 年加息后的贷款，违约预测还能准吗？OOT 答的就是这个问题。OOT 上的 AUC 衰退幅度（AUC_train − AUC_OOT）越小，跨周期泛化能力越强。

**核心约束（防止数据泄露）**

- ⚠️ 合成样本由 2022Q1–Q3 的真实违约分布驱动生成，**仅注入 Train 和 OOS**
- ⚠️ OOT 集全程不参与 GAN 训练、不参与超参调优、**不含一个合成样本**
- ⚠️ 如果先生成再划分，合成样本混入 OOT → AUC 虚高 → 跨周期结论无效

---

## 建议的讲稿（1 分钟口述版）

> 本页介绍两个核心设计：算法怎么跑、数据怎么验证跨周期泛化。
>
> **左边是算法框架**。整个方法分三步——Phase 1 从右删失违约样本中提取违约倾向得分 r，Phase 2 训练基分类器定位决策边界并通过知识蒸馏使其可微分，Phase 3 把 r 和边界信息同时注入生成器，用三项损失联合优化，让生成器定向产出边界附近的合成违约样本。第一章 GAN 阶段内部会做 80/20 划分用于生成质量自检，这个我不展开。
>
> **右边是跨周期验证的核心设计**。第一章输出均衡训练集后，第二章把它切成 Train（80%）和 OOS（20%），分别用于调参和同分布测试。但这两个集合分不出高低——真正区分方法好坏的是 OOT。OOT 是 2023Q1-Q3 的全部完整观测数据，独立于训练集，不含任何一个合成样本。它回答的问题很干净：用低利率期数据训练的模型，面对加息后的贷款，违约预测还能准吗？OOT 上的 AUC 才是这篇论文最核心的指标，AUC 衰退幅度越小，跨周期泛化越强。

---

## 页面上建议放的关键数字

| 数字 | 含义 | 位置 |
|---|---|---|
| 80/20 | Train/OOS 切分比例 | 第二章划分 |
| **0** | OOT 中合成样本数量 | 高亮标注 |
| 2023Q1–Q3 | OOT 独立时间窗 | OOT 行 |
| **ΔAUC** | = AUC_OOS − AUC_OOT，跨周期衰退幅度 | 论文核心指标 |
| r=0.983 | 知识蒸馏效果 | Phase 2 |
