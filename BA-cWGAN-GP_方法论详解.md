# 边界感知条件 WGAN-GP（BA-cWGAN-GP）方法论详解

## 目录

1. [问题背景：100 个灯泡的直觉理解](#一问题背景100-个灯泡的直觉理解)
2. [数据洞察：被浪费的右删失样本](#二数据洞察被浪费的右删失样本)
3. [算法总览：三阶段框架](#三算法总览三阶段框架)
4. [Phase 1：违约倾向得分——从右删失样本中"榨取"信息](#四phase-1违约倾向得分从右删失样本中榨取信息)
5. [Phase 2：边界距离——识别哪些违约样本最有价值](#五phase-2边界距离识别哪些违约样本最有价值)
6. [Phase 3：BA-cWGAN-GP——定向生成边界附近的合成样本](#六phase-3ba-cwgan-gp定向生成边界附近的合成样本)
7. [完整伪代码](#七完整伪代码)
8. [数据划分策略：GAN 阶段与模型阶段的两层划分](#八数据划分策略gan-阶段与模型阶段的两层划分)
9. [第一章如何证明优越性：三章的分工与实验设计](#九第一章如何证明优越性三章的分工与实验设计)
10. [常见疑问](#十常见疑问)

---

## 一、问题背景：100 个灯泡的直觉理解

假设你要训练一个质检员（分类器）判断灯泡是否合格：

- 流水线上 1000 个灯泡，只有 40 个是次品（IR=24，和你的数据差不多）
- 质检员只看过这 40 个次品的照片，经验严重不足
- 你想**生成一些假次品照片**给质检员练手

### 标准 WGAN-GP 的做法（盲生成）

WGAN-GP 学到 40 个次品的大致分布，然后**随机生成** 200 个新次品。问题是：

```
生成的 200 个次品中：
├── 120 个长得"一看就是次品"（碎灯泡、不亮灯丝）── 质检员不看也会判对
├── 50 个长得"和正品有区别但不大" ── 有点训练价值
└── 30 个长得"几乎像正品" ── 最有训练价值！但要靠运气才生成得到
```

**核心浪费**：大部分计算资源和生成样本，都花在了"质检员本来就会判断"的区域。

### 本文方法（边界感知生成）的做法

与其随机生成，不如**告诉生成器：重点生成那些"质检员最拿不准"的样本**。

同时，我们手中还有一批"额外线索"——2023Q4+ 的 8,150 笔纯违约样本。它们虽然不能直接用于训练（因为不知道对应的正常样本在哪），但它们携带了"违约速度模式"的信息。通过训练一个违约速度分类器（speed_clf）从这批样本的静态特征中推断出违约倾向得分 r，就可以**用 r 告诉生成器：生成具有这类风险特征的样本，而不是盲目随机生成。**

---

## 二、数据洞察：被浪费的右删失样本

### 你的数据中两类违约样本的本质差异

| 特征 | 2022 年的违约样本 | 2023Q4+ 的违约样本 |
|---|---|---|
| 观测时长 | 满 24 个月 | 仅 2–23 个月 |
| 违约时间 | 可能在 1–24 个月内任何时间违约 | 观测到的违约均在 ≤23 个月内 |
| 能用于训练吗？ | 可以（知道它违约了） | 不能（非违约样本也观测不足） |
| 有什么特殊价值？ | 违约快慢可精确标注 | **违约速度未知，但静态特征中隐含违约模式信息** |

**关键洞察**：

> 一笔在贷款发放后第 5 个月就逾期的贷款，和一笔在第 23 个月才逾期的贷款，虽然都被标为 DFlag=1，但它们的风险模式完全不同。2023Q4+ 的样本因为观测窗口不足，其真实的违约速度（Early 还是 Late）无法直接判定——发放越晚的季度，观测窗口越短，越只能捕获到极端早期的违约事件，晚期违约被系统性遮蔽。但这些样本的静态特征（信用评分、DTI、贷款目的等）中隐含了可用于推断违约倾向的信号，直接丢弃意味着这些信号完全损失。

### 传统做法 vs 本文做法

```
传统做法：
  2023Q4+ 纯违约样本 ──→ 直接丢弃 ✗

本文做法：
  2023Q4+ 纯违约样本 ──→ 训练 speed_clf 提取违约速度模式 ──→ 推断 r 作为生成器的引导信号 ✓
```

---

## 三、算法总览：三阶段框架

整个方法分为三个 Phase，前两个 Phase 是"信息提取"，第三个 Phase 是"核心生成"。

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           输入数据                                        │
│                                                                           │
│  ① 2022Q1–2023Q3 完整观测数据（训练集）                                   │
│     - 约 1,015,587 笔贷款，含 DFlag 标签                                  │
│     - 违约样本可以追溯到"首次违约的月份"                                   │
│                                                                           │
│  ② 2023Q4–2025Q2 纯违约数据（右删失）                                     │
│     - 8,150 笔纯违约贷款                                                   │
│     - 没有 DFlag=0 的对照样本，无法直接用于训练                            │
│     - 违约速度（Early/Late）因观测窗口不足而未知，需由 speed_clf 推断      │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Phase 1：违约倾向得分提取                                                │
│                                                                           │
│  输入：2022Q1–Q3 违约样本（带违约时间标签）+ 2023Q4+ 纯违约样本           │
│  操作：训练一个二分类器区分 "early default" vs "late default"              │
│  输出：每笔贷款（含纯违约样本）的"违约倾向得分 r ∈ [0,1]"                 │
│        r 越大 = 越像快速违约的高风险模式                                   │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Phase 2：决策边界识别                                                    │
│                                                                           │
│  输入：2022Q1–Q3 完整训练数据                                             │
│  操作：训练一个基础 XGBoost 分类器，对每个少数类样本计算预测概率 p         │
│  输出：每笔违约样本的"边界距离 d = |p - 0.5|"                             │
│        d 越小 = 分类器越拿不准 = 越值得生成类似样本                        │
└──────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  Phase 3：BA-cWGAN-GP 定向生成                                            │
│                                                                           │
│  输入：                                                                   │
│    - 训练集少数类样本（带 r 标签 + d 权重）                                │
│    - 随机噪声 z                                                           │
│  生成器：G(z | r) → 合成样本 x_fake，条件是违约倾向得分 r                  │
│  判别器：D(x) → 判断是真实样本还是生成的                                   │
│  边界损失：L_boundary = -|分类器对 x_fake 的预测 - 0.5|                    │
│  输出：高质量合成违约样本，集中于决策边界附近                               │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 四、Phase 1：违约倾向得分——从右删失样本中"榨取"信息

### 4.1 原始数据回顾：Performance 月度记录

Freddie Mac 的 Performance 数据是**每月一条记录**。以一笔 2022Q1 发放的贷款为例：

| 月份 | Loan_Age | Delinquency_Status | 说明 |
|---|---|---|---|
| 2022-02 | 1 | 0 | 正常还款 |
| 2022-03 | 2 | 0 | 正常 |
| ... | ... | ... | ... |
| 2023-05 | 16 | 1 | 逾期 30 天 |
| 2023-06 | 17 | 2 | 逾期 60 天 |
| 2023-07 | 18 | 3 | **逾期 90 天 → 触发违约** |

这笔贷款的"首次违约月龄"就是 18 个月。这个信息在聚合时丢失了（只保留了 max_delinq_win），但可以从原始月度数据中恢复。

### 4.2 构建违约时间标签

对 2022Q1–2023Q3 中所有 DFlag=1 的贷款，回溯原始 Performance 数据：

```
对每笔违约贷款：
  遍历其所有月度记录（按 Loan_Age 升序），
  找到第一个 DLQ ≥ 3 或 ZBC ∈ {03, 09} 的月份，
  记录此时的 Loan_Age 作为 "first_default_month"
```

然后贴上标签：

| 标签 | 条件 | 含义 | 2022Q1–Q3 中数量（估计） |
|---|---|---|---|
| **Early Default** | first_default_month ≤ 12 | 一年内就违约，快速恶化 | ~15,000 |
| **Late Default** | first_default_month ∈ (12, 24] | 一年后才违约，缓慢恶化 | ~20,000 |

### 4.3 训练"违约速度分类器"

现在，用一个 XGBoost 分类器来学习"什么特征组合容易导致快速违约"：

```python
# 训练数据：2022Q1–Q3 所有违约样本
# X = 79 个特征（和主模型一样）
# y = 1 if Early Default, 0 if Late Default

speed_clf = XGBoostClassifier()
speed_clf.fit(X_train_defaults, y_speed_label)
```

### 4.4 对右删失样本打分

训练好的 `speed_clf` 对 2023Q4+ 的 8,150 笔纯违约样本预测：

```python
# 预测每笔贷款属于 "Early Default" 的概率
r = speed_clf.predict_proba(X_censored)[:, 1]
# r ∈ [0, 1]，r 越大 = 越像快速违约的高风险模式
```

同时对 2022Q1–Q3 的违约样本也计算 r（用于 Phase 3 的条件生成）：

```python
r_train = speed_clf.predict_proba(X_train_defaults)[:, 1]
```

### 4.5 这一步的本质

```
         右删失样本的原始状态：
         "我知道它是违约，但我不知道它是第几个月违约的"
                          │
         speed_clf 推断  │  基于完整观测违约样本中学到的"特征→违约速度"映射关系
                          ▼
         右删失样本的新状态：
         "这笔贷款有 87% 的概率属于快速违约型"
         → 这个 0.87 就是 r，作为后续生成的引导信号
```

**为什么这比扔掉好？**

因为你扔掉 8,150 条数据，就永远失去了"这批违约样本的静态特征在 2023–2025 年间分布如何"的信息。通过 speed_clf，你以 r 的形式从静态特征中间接提取了违约倾向信号——不是直接知道违约月份，而是知道**给定特征下快速违约的后验概率**。

---

## 五、Phase 2：边界距离——识别哪些违约样本最有价值

### 5.1 决策边界的直觉

想象二维平面上分布着正类和负类样本：

```
        ○ ○ ○ ○ ○
      ○ ○ ○ ○ ○ ○ ○          ○ = 正常样本（多数类）
    ○ ○ ○ ○ ○ ○ ○ ○
   ○ ○ ○ ○ ○ ○ ○ ○ ●         ● = 违约样本（少数类）
  ○ ○ ○ ○ ○ ○ ○ ● ● ●
  ○ ○ ○ ○ ○ ○ ○ ● ● ●         ← 决策边界（分类器划的线）
  ○ ○ ○ ○ ○ ○ ● ● ● ●
  ○ ○ ○ ○ ○ ● ● ● ● ●
        ● ● ● ● ●
           ● ● ●

  远离边界的 ● → 分类器非常确信它是违约 → 生成意义小
  靠近边界的 ● → 分类器对它最犹豫 → 生成价值最大
```

### 5.2 计算边界距离

在完整训练集上训练一个基础 XGBoost：

```python
base_clf = XGBoostClassifier()
base_clf.fit(X_train, y_train)

# 对每个违约样本预测概率
p = base_clf.predict_proba(X_train)[:, 1]  # p ∈ [0, 1] 是"违约概率"

# 边界距离
d = abs(p - 0.5)  # d ∈ [0, 0.5]
```

| p 值 | d 值 | 含义 | 生成优先级 |
|---|---|---|---|
| 0.92 | 0.42 | 分类器很确定它是违约 | 低 |
| 0.55 | 0.05 | 分类器很犹豫，几乎要判为正常 | **高** |
| 0.48 | 0.02 | 分类器差点判为正常 | **最高** |

### 5.3 用于加权采样

在 Phase 3 训练生成器时，不是均匀采样真实违约样本，而是**按边界距离加权采样**：

```python
# 距离越小 → 权重越大
weight = 1 / (d + ε)  # ε = 0.01，防止除零

# d=0.4 → weight=2.4
# d=0.05 → weight=16.7  ← 边界样本被采样的概率高 7 倍
```

这样生成器在训练时**频繁看到边界附近的违约样本**，自然学会生成类似的特征模式。

---

## 六、Phase 3：BA-cWGAN-GP——定向生成边界附近的合成样本

### 6.1 WGAN-GP 的最简回顾（你需要知道的）

一个标准的 WGAN-GP 有两个网络：

```
生成器 G(z)：
  输入：随机噪声 z（从正态分布采样，比如 100 维）
  输出：合成的特征向量 x_fake（79 维，和你的数据一样）

判别器 D(x)：
  输入：一个特征向量 x（79 维）
  输出：一个实数（不是概率！），越大 = 越像真实样本
```

训练过程（简化版）：

```
每一步：
  1. 生成一批假样本：x_fake = G(z)
  2. 判别器给真假打分：score_real = D(x_real), score_fake = D(x_fake)
  3. 判别器损失：L_D = score_fake - score_real + GP（梯度惩罚项）
     → 判别器学会：真样本打高分，假样本打低分
  4. 生成器损失：L_G = -score_fake
     → 生成器学会：让判别器给假样本打高分（以假乱真）
  5. 交替训练，最终 G 能把噪声变成"看得像真的"样本
```

**梯度惩罚（Gradient Penalty, GP）** 是 WGAN-GP 的核心改进：
- 在真假样本之间的连线上随机插值
- 要求判别器在该点上的梯度范数接近 1
- 效果：训练更稳定，不易崩溃

### 6.2 本文的两项改进（用一张图说明）

```
标准 WGAN-GP 的生成器输入：          本文 BA-cWGAN-GP 的生成器输入：

     z（噪声）                            z（噪声）  +  r（违约倾向）
       │                                     │           │
       ▼                                     └─────┬─────┘
   ┌───────┐                                      ▼
   │   G   │                               ┌───────────┐
   └───┬───┘                               │     G     │
       │                                   └─────┬─────┘
       ▼                                         │
   x_fake（随便生成，                        x_fake（知道要生成什么
   落在哪算哪）                              "类型" 的违约样本）
                                               │
                                               ▼
  标准损失：                           本文损失：
  L_G = -D(G(z))                       L_G = -D(G(z|r))
                                          + λ_boundary × L_boundary
                                          + λ_r × L_r

                                        其中：
                                        L_boundary = -|base_clf(x_fake) - 0.5|
                                        （惩罚远离边界的生成）
                                        
                                        L_r = |speed_clf(x_fake) - r|
                                        （惩罚生成样本的违约速度偏离预期）
```

### 6.3 改进一：条件生成（Conditional Generation）

**动机**：标准 WGAN-GP 的生成器不知道我们要"什么类型的违约样本"。

**做法**：把 Phase 1 的违约倾向得分 r 拼接到噪声 z 后面，作为生成器的输入。

```python
# 标准 WGAN-GP
z = torch.randn(batch_size, 100)        # 100 维噪声
x_fake = generator(z)                    # 盲生成

# 本文 BA-cWGAN-GP
z = torch.randn(batch_size, 100)        # 100 维噪声
r = torch.rand(batch_size, 1)           # 违约倾向得分（条件变量）
z_cond = torch.cat([z, r], dim=1)      # 拼接，101 维
x_fake = generator(z_cond)             # 条件生成
```

**训练细节**：每轮从训练集的 r 分布中随机采样 r 值，与对应的真实样本配对训练。这样生成器学会：给定 r=0.9，生成"快速违约型"样本；给定 r=0.3，生成"缓慢违约型"样本。

### 6.4 改进二：边界感知损失（Boundary-Aware Loss）

**动机**：即便有条件引导，生成器也不知道哪些特征区域对分类器最有价值。

**做法**：在生成器损失中加一项——如果生成的样本远离分类器的决策边界，就惩罚。

```python
# Phase 2 训练的 base_clf（冻结参数，不更新）
base_clf.eval()

def boundary_loss(x_fake):
    """
    计算生成样本到决策边界的距离。
    距离越近，loss 越小；距离越远，loss 越大。
    """
    prob = base_clf.predict_proba(x_fake)[:, 1]   # 分类器看它像违约的概率
    distance = torch.abs(prob - 0.5)                # 到 0.5 的距离
    # 我们想让 distance 小 → loss 大 → 但这里需要"远离边界则惩罚"
    # 所以取负号：distance 越大 → loss 越大（越惩罚）
    return -distance.mean()  # 等价于让生成器 minimize distance
```

**这个损失的直观含义**：

```
base_clf 对 x_fake 的预测：

  预测 = 0.95（"这肯定是违约"） → distance = 0.45 → loss = -0.45（被惩罚！）
  预测 = 0.55（"可能是违约吧"） → distance = 0.05 → loss = -0.05（几乎不惩罚）
  预测 = 0.50（"完全拿不准"）   → distance = 0.00 → loss =  0.00（理想！）

  → 生成器被逼着往分类器最不确定的区域生成样本
```

### 6.5 完整损失函数

**判别器损失**（与标准 WGAN-GP 一致）：

$$\mathcal{L}_D = \mathbb{E}[D(\tilde{x})] - \mathbb{E}[D(x)] + \lambda_{gp} \cdot GP$$

其中 GP（梯度惩罚）：

$$GP = \mathbb{E}[(\|\nabla_{\hat{x}} D(\hat{x})\|_2 - 1)^2]$$

$\hat{x}$ 是真假样本之间的随机插值点。

**生成器损失**（本文新增后两项）：

$$\mathcal{L}_G = -\mathbb{E}[D(G(z|r))] + \lambda_{boundary} \cdot \mathcal{L}_{boundary} + \lambda_{r} \cdot \mathcal{L}_{r}$$

其中：

| 项 | 公式 | 作用 | λ 建议值 |
|---|---|---|---|
| 对抗损失 | $-\mathbb{E}[D(G(z\|r))]$ | 让生成样本"以假乱真" | — |
| 边界损失 | $-\mathbb{E}[\|base\_clf(G(z\|r)) - 0.5\|]$ | 聚焦决策边界 | λ_boundary=0.1 |
| 违约倾向损失 | $\mathbb{E}[\|speed\_clf(G(z\|r)) - r\|]$ | 约束违约速度模式 | λ_r=0.05 |

### 6.6 生成与筛选流程

训练完成后，生成合成违约样本：

```
1. 从训练集的 r 分布中随机采样目标 r 值（反映真实违约倾向分布）
2. 随机采样噪声 z
3. 拼接 [z, r] 输入生成器 → 得到 x_fake
4. 用 base_clf 检查 x_fake 的质量：
   - 如果 base_clf(x_fake) ∈ [0.3, 0.7]，保留（边界附近，高质量）
   - 如果 base_clf(x_fake) > 0.7 或 < 0.3，丢弃（远离边界，低质量）
5. 重复直到生成够用数量
```

这个后筛选步骤是可选的，它确保即使训练中有些样本偏离了目标，最终进入训练集的合成样本也全部集中在边界附近。

---

## 七、完整伪代码

```python
# ===========================================================================
# BA-cWGAN-GP 完整流程
# ===========================================================================

# ------------------------------------------------------------------
# Phase 1: 违约倾向得分
# ------------------------------------------------------------------
# Step 1.1: 从 Performance 月度数据中提取首次违约月份
for loan_id in default_loans:
    perf_records = load_all_monthly_records(loan_id)
    sorted_by_age = sort(perf_records, by="Loan_Age")
    for record in sorted_by_age:
        if record.DLQ >= 3 or record.ZBC in ["03", "09"]:
            first_default_month = record.Loan_Age
            break

# Step 1.2: 贴标签
y_speed = []
for loan in default_loans:
    if loan.first_default_month <= 12:
        y_speed.append(1)  # Early Default
    else:
        y_speed.append(0)  # Late Default

# Step 1.3: 训练"违约速度分类器"
speed_clf = XGBoostClassifier()
speed_clf.fit(X[default_loans], y_speed)

# Step 1.4: 对右删失样本打分
r_train = speed_clf.predict_proba(X[default_loans])[:, 1]    # 训练集违约样本
r_censored = speed_clf.predict_proba(X[censored_loans])[:, 1]  # 右删失样本

# ------------------------------------------------------------------
# Phase 2: 决策边界识别
# ------------------------------------------------------------------
base_clf = XGBoostClassifier()
base_clf.fit(X_train, y_train)  # 完整训练集

# 对每个少数类样本
p = base_clf.predict_proba(X_train_minority)[:, 1]
d = abs(p - 0.5)                              # 边界距离
sample_weight = 1.0 / (d + 0.01)              # 加权

# ------------------------------------------------------------------
# Phase 3: BA-cWGAN-GP 训练
# ------------------------------------------------------------------
G = Generator(input_dim=101)   # 100(z) + 1(r) = 101
D = Discriminator(input_dim=79)

for epoch in range(n_epochs):
    for batch_real, batch_r in data_loader:  # 按 sample_weight 加权采样
        # ---- 训练判别器 ----
        z = torch.randn(batch_size, 100)
        z_cond = torch.cat([z, batch_r.unsqueeze(1)], dim=1)
        x_fake = G(z_cond)

        # 梯度惩罚
        eps = torch.rand(batch_size, 1)
        x_hat = eps * batch_real + (1 - eps) * x_fake
        gp = gradient_penalty(D, x_hat)

        L_D = D(x_fake).mean() - D(batch_real).mean() + 10.0 * gp
        D_optimizer.step(L_D)

        # ---- 每 n_critic 步训练一次生成器 ----
        if step % 5 == 0:
            z = torch.randn(batch_size, 100)
            z_cond = torch.cat([z, batch_r.unsqueeze(1)], dim=1)
            x_fake = G(z_cond)

            # 对抗损失
            L_adv = -D(x_fake).mean()

            # 边界感知损失（新增！）
            prob_fake = base_clf.predict_proba(x_fake)[:, 1]
            L_boundary = -abs(prob_fake - 0.5).mean()

            # 违约倾向一致性损失（新增！）
            r_pred = speed_clf.predict_proba(x_fake)[:, 1]
            L_r = abs(r_pred - batch_r).mean()

            L_G = L_adv + 0.1 * L_boundary + 0.05 * L_r
            G_optimizer.step(L_G)

# ------------------------------------------------------------------
# 生成 + 筛选
# ------------------------------------------------------------------
synthetic_samples = []
while len(synthetic_samples) < target_count:
    z = torch.randn(1024, 100)
    r_sampled = sample_from_empirical_r_distribution(1024)  # 真实 r 分布
    z_cond = torch.cat([z, r_sampled.unsqueeze(1)], dim=1)
    x_fake = G(z_cond)

    prob = base_clf.predict_proba(x_fake)[:, 1]
    mask = (prob >= 0.3) & (prob <= 0.7)  # 只保留边界附近的
    synthetic_samples.extend(x_fake[mask])

synthetic_samples = synthetic_samples[:target_count]
```

### 超参数建议

| 参数 | 建议值 | 说明 |
|---|---|---|
| z 维度 | 100 | 噪声维度，标准取值 |
| r 维度 | 1 | 违约倾向得分 |
| G 网络结构 | 100→256→512→256→79 | 全连接 + BatchNorm + LeakyReLU |
| D 网络结构 | 79→256→512→256→1 | 全连接 + LeakyReLU |
| λ_gp | 10.0 | 梯度惩罚系数 |
| λ_boundary | 0.1 | 边界损失权重 |
| λ_r | 0.05 | 违约倾向一致权重 |
| n_critic | 5 | 判别器每训练 5 步，生成器训练 1 步 |
| batch_size | 256 | 批次大小 |
| 训练轮数 | 2000–5000 | 视收敛情况 |
| D/G 学习率 | 1e-4 | Adam 优化器 |

---

## 八、数据划分策略：GAN 阶段与模型阶段的两层划分

你的直觉是对的——**训练集 / 测试集 / OOT 的划分属于第二章模型预测阶段的工作**。但在 GAN 数据生成阶段，同样需要一个内部的"训练 / 留出"划分。两者的**目的不同**。

### 8.1 为什么 GAN 阶段也必须划分

不是为了模型评估，而是为了解决一个更基本的问题：**你怎么判断 GAN 生成的东西好不好？**

- **不划分的后果**：Phase 2 的 base_clf 在全部数据上训练，再在全部数据上评估边界损失 → 分类器已经"见过"所有样本，边界判定不可信，生成质量评估自欺欺人
- **划分之后**：base_clf 在 GAN 训练集上训练，GAN 验证集的真实违约样本留作"标杆"——用它来算 Wasserstein 距离、特征相关系数差异，检查生成器是否真的学到了分布，还是只是过拟合

另外，**合成样本绝不能污染测试集和 OOT 集**。如果先生成、再划分，合成样本就会混入测试集和 OOT 集，AUC 虚高毫无意义。

### 8.2 两层划分的全景图

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                   │
│   全部完整观测数据 (2022Q1–2023Q3): ~1,015,587 条                  │
│   右删失违约集 (2023Q4–2025Q2): 8,150 条                           │
│                                                                   │
└────────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────┐
│            第一层划分：GAN 数据生成阶段（第一章）                    │
│                                                                    │
│  完整观测数据 (2022Q1–Q3)                                           │
│  │                                                                 │
│  ├── GAN 训练集 (80%)                                               │
│  │   │                                                             │
│  │   ├── Phase 1: 训练 speed_clf（含右删失违约集打分）               │
│  │   ├── Phase 2: 训练 base_clf（边界识别）                         │
│  │   ├── Phase 3: 训练 BA-cWGAN-GP（加权采样真实样本）              │
│  │   └── 输出: 合成违约样本                                         │
│  │                                                                 │
│  └── GAN 验证集 (20%)                                               │
│      └── 仅用于生成质量评估指标（边界富集度、Wasserstein距离等）     │
│         不参与 GAN 训练，也不加合成样本                               │
│                                                                    │
│  最终输出：合成违约样本 + GAN 训练集真实样本 → 均衡训练集             │
│                                                                    │
└────────────────────────────────┬─────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────┐
│           第二层划分：Stacking 模型预测阶段（第二章）                │
│                                                                    │
│  均衡训练集（第一章输出的）                                          │
│  │                                                                 │
│  ├── 训练集 Train (80%) ──→ 5折交叉验证调参                         │
│  ├── 测试集 Test  (20%) ──→ 同分布性能评估                          │
│  └── OOT 测试集 ──→ 2023Q1–Q3 完整数据（独立于 GAN 阶段，           │
│                    不含任何合成样本）→ 跨时期泛化能力验证             │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
```

### 8.3 实际操作顺序

```
Stpe 1: 锁定全部完整观测数据 (2022Q1–Q3) + 右删失违约集 (2023Q4+)
Step 2: 从完整观测中划出 GAN 训练集 80% → Phase 1→2→3 → 生成合成样本
Step 3: 合成样本 + GAN训练集真实样本 → 均衡训练集（第一章输出）
Step 4: GAN 验证集 20%（不放回，不加合成样本）→ 仅用于生成质量评估指标
Step 5: 把均衡训练集交给第二章做 Stacking
Step 6: 第二章内部再按 8:2 划分 train/test，额外加上 OOT
Step 7: 测试集和 OOT 集始终不包含任何合成样本
```

### 8.4 一个容易混淆的点

GAN 阶段的 80/20 划分和模型阶段的 80/20 划分各自独立。两个划分都可能用到 2022Q1–Q3 的数据，但**第一章关心生成质量，第二章关心预测性能**。最终 OOT 集（2023Q1–Q3）的 AUC 才是对你的论文最核心的证明——它能回答"这个模型在加息周期真的比低利率期表现好吗"。

---

## 九、第一章如何证明优越性：三章的分工与实验设计

一个常见的误解是：第一章和第二章都是用 LR / RF / XGBoost / LightGBM 对比 AUC、F1。如果两章都这样写，审稿人会问——**你的创新到底在采样还是在模型？**

关键区别在于**控制变量不同**。

### 9.1 三章各回答一个问题

| 章节 | 回答的问题 | 控制变量 | 比较对象 | 分类器的角色 |
|---|---|---|---|---|
| **第一章** | 哪种采样方法最好？ | 固定 XGBoost | 6 种采样方法 | XGBoost 是"裁判" |
| **第二章** | 哪种集成模型最好？ | 固定 BA-cWGAN-GP | 4 个基学习器 + Stacking | 各模型是"选手" |
| **第三章** | 模型为什么这样判？ | 固定最优模型 | 不同特征的 SHAP 值 | 解释对象 |

### 9.2 第一章的实验设计：固定裁判，换采样方法

**核心逻辑**：用同一个分类器（XGBoost），在不同采样方法处理过的数据上训练，看谁的 AUC 最高。XGBoost 在这里只是一个"裁判"——它不是你的最终模型，它只是一个公平的尺子。

```python
# 第一章的统一评估框架
for sampling_method in [None, SMOTE, GAN, WGAN-GP, cWGAN-GP, BA-cWGAN-GP]:
    X_balanced, y_balanced = sampling_method.fit_resample(X_train, y_train)
    
    # 固定同一个分类器（裁判）
    clf = XGBoostClassifier(same_hyperparams)  
    clf.fit(X_balanced, y_balanced)
    
    # 在同一个测试集上评估（测试集不含合成样本！）
    auc, f1, ks = evaluate(clf, X_test, y_test)
```

第一章最终产出的表大概是这样的：

| 采样方法 | AUC | F1 | KS | Brier Score | 边界富集度 |
|---|---|---|---|---|---|
| 不采样（Baseline） | 0.82 | 0.31 | 0.52 | 0.12 | — |
| SMOTE | 0.85 | 0.38 | 0.56 | 0.10 | — |
| GAN | 0.83 | 0.34 | 0.53 | 0.14 | 0.15 |
| WGAN-GP | 0.86 | 0.40 | 0.58 | 0.09 | 0.22 |
| cWGAN-GP（仅条件） | 0.87 | 0.42 | 0.60 | 0.08 | 0.28 |
| **BA-cWGAN-GP** | **0.89** | **0.44** | **0.62** | **0.07** | **0.45** |

**结论的写法**："在固定 XGBoost 分类器的条件下，BA-cWGAN-GP 生成均衡数据训练的模型在全部指标上优于其他采样方法。边界富集度达到 0.45，说明生成器有效聚焦于决策边界。这验证了条件引导和边界感知两项改进各自的有效性。"

### 9.3 消融实验：量化每项改进的贡献

| 配置 | 条件引导 r | 边界损失 L_boundary | 违约倾向损失 L_r | 含义 | 预期 AUC |
|---|---|---|---|---|---|
| A | ✗ | ✗ | ✗ | = 标准 WGAN-GP | 0.86 |
| B | ✓ | ✗ | ✗ | = cWGAN-GP，仅加条件引导 | 0.87 |
| C | ✗ | ✓ | ✗ | 仅加边界感知，无条件引导 | 0.87 |
| D | ✓ | ✓ | ✗ | 条件 + 边界，去掉 L_r | 0.88 |
| **E** | **✓** | **✓** | **✓** | **完整 BA-cWGAN-GP** | **0.89** |

消融实验的逻辑链路：

```
A → B：AUC +0.01 → 条件引导 (r) 的独立贡献
A → C：AUC +0.01 → 边界感知损失的独立贡献
B → D：AUC +0.01 → 边界感知 + 条件引导的协同贡献
D → E：AUC +0.01 → L_r（违约倾向一致性约束）的边际贡献
```

### 9.4 第二章的实验设计：固定采样方法，换模型

第一章的结论是"BA-cWGAN-GP 采样最好"。第二章就用这个采样结果，去比模型。

```python
# 第二章的统一评估框架
X_balanced, y_balanced = BA_cWGAN_GP.fit_resample(X_train, y_train)

for model in [LR, RF, XGBoost, LightGBM, Stacking]:
    model.fit(X_balanced, y_balanced)
    auc, f1, ks = evaluate(model, X_test, y_test)
    auc_oot, f1_oot, ks_oot = evaluate(model, X_OOT, y_OOT)
```

第二章产出的表大概是这样的：

| 模型 | AUC (同分布) | AUC (OOT) | F1 (OOT) | KS (OOT) |
|---|---|---|---|---|
| LR | 0.84 | 0.82 | 0.34 | 0.51 |
| RF | 0.87 | 0.86 | 0.41 | 0.58 |
| XGBoost | 0.89 | 0.88 | 0.44 | 0.62 |
| LightGBM | 0.89 | 0.88 | 0.44 | 0.62 |
| **Stacking 融合** | **0.91** | **0.90** | **0.47** | **0.65** |

### 9.5 为什么两章不冲突

```
第一章的创新点：  BA-cWGAN-GP 采样方法                      [新方法]
第二章的创新点：  Stacking 集成（将优质数据的效果最大化）      [新组合]
第三章的创新点：  SHAP 解释 + 跨周期特征漂移分析               [新视角]

三章的因果关系：
  第一章产生"更好的训练数据" → 第二章用它训练"更好的模型" → 第三章解释它为什么好
```

审稿人的逻辑链是：
1. 第一章证明 BA-cWGAN-GP 优于 SMOTE/WGAN-GP → **采样方法有创新**
2. 第二章证明在 BA-cWGAN-GP 数据上 Stacking 优于单一模型 → **集成策略有效**
3. 两个创新**正交叠加**：换掉采样方法，Stacking 的优势依然成立；换掉 Stacking，采样方法的优势依然成立

### 9.6 评价指标汇总

**分类性能**（第一章和第二章共用）：
- AUC、KS 统计量、F1-score、Brier Score

**生成质量**（第一章特有）：

| 指标 | 含义 | 计算方法 |
|---|---|---|
| 边界富集度 | 生成样本中 d<0.2 的比例 | 越高越好 |
| 分布相似度 | 生成样本与真实违约样本的 Wasserstein 距离 | 越低越好 |
| 特征相关性保持 | 生成样本各特征的协方差矩阵与真实样本的差异 | Frobenius 范数 |

**跨周期稳健性**（第二章特有）：
- OOT AUC 衰退幅度 = AUC_train − AUC_OOT，越小越稳健

### 9.7 第三章的补充实验

对最优模型做 SHAP 全局 + 局部解释，额外增加一个验证生成质量的实验：
- 对比"真实边界违约样本"和"合成边界违约样本"的 SHAP 值分布
- 如果两者 Top-10 特征的 SHAP 排序一致 → 生成器没有引入虚假模式 → 生成样本可靠

---

## 十、常见疑问

### Q1：Phase 1 的 speed_clf 和 Phase 2 的 base_clf 用途有什么不同？

| | speed_clf（Phase 1） | base_clf（Phase 2） |
|---|---|---|
| 预测目标 | Early vs Late Default | Default vs Non-Default |
| 训练数据 | 仅违约样本 | 全部数据 |
| 作用 | 提取"违约速度模式" | 定位"决策边界" |
| 在 Phase 3 中 | 提供条件变量 r + L_r 约束 | 提供边界损失 L_boundary + 生成后筛选 |

两个分类器各司其职，互不干扰。

### Q2：如果右删失样本数量很少（比如只有几百条），Phase 1 还有意义吗？

有意义，但效果会打折扣。极端情况下（< 100 条），可以选择跳过 Phase 1，退化为仅用 Phase 2 边界感知的纯 WGAN-GP（消融实验 C），此时条件变量 r 退化为不使用的状态。

对你来说，8,150 条足够用了。

### Q3：为什么不直接用 2023Q4+ 的纯违约样本过采样？它们不也是违约样本吗？

不能用，因为这些样本**没有对应的非违约样本做对照**。如果把这些纯违约样本直接放入训练集，你需要给它们配 DFlag=0 的对照样本，但对照样本因为观测不足被剔除了——这是右删失的核心矛盾。

本文的思路是**间接利用**：不把它们当训练样本，而是从中提取"违约倾向模式"来引导生成器。

### Q4：和 Mushava & Murray (2024) 的关系是什么？

本文的 BA-cWGAN-GP 是**数据层面**的方法（过采样），Mushava & Murray 的 flexible loss 是**模型训练层面**的方法（损失函数）。

两者**正交**——你可以：
1. 用 BA-cWGAN-GP 生成均衡训练集
2. 在 XGBoost 中使用 Mushava & Murray 的 CE loss + GEV link

这样两个创新点互不冲突，1+1>2。

### Q5：代码实现复杂吗？

核心改动就三处：
1. `generator.forward()` 输入端多加了一维 r：约 1 行改动
2. 生成器损失函数加了两项：约 5 行新代码
3. 训练时按 sample_weight 加权采样：约 3 行新代码

其余跟标准 WGAN-GP 完全一致。

### Q6：第一章的"XGBoost 裁判"和第二章的"XGBoost 选手"是同一个模型吗？会不会导致数据泄露？

不是同一个。第一章的 XGBoost 在**GAN 训练集**（80% 完整观测的子集）上训练，用于给 Phase 2 提供边界距离和 Phase 3 提供边界损失。第二章的 XGBoost 在**均衡训练集**（GAN 训练集 + 合成样本）上重新训练，重新调参。

两者训练数据不同、随机种子不同、超参数可能不同——只是模型结构一样。不存在泄露。

---

## 论文标题建议

原文：*跨经济周期的信贷风险预测研究——基于WDCGGAN的Stacking集成模型*

建议修改为：

> **跨经济周期的信贷风险预测研究——基于边界感知条件WGAN-GP的Stacking集成模型**

或英文：

> *Cross-Cycle Credit Risk Prediction: A Boundary-Aware Conditional WGAN-GP Stacking Ensemble Approach*

主要改动：
- WDCGGAN → BA-cWGAN-GP（去掉无意义的卷积，加上有意义的边界感知）
- 如果坚持保留 GAN 变体的缩写，可简称为 **BA-WGAN** 或 **BCW-GAN**（Boundary-aware Conditional Wasserstein GAN）

---

*文档版本：v2.0 | 2026-05-18*
