# 数据预处理

## 一、数据来源

本文使用的数据来源于房地美（Freddie Mac）单户贷款级数据集（Single-Family Loan-Level Dataset）。该数据集是美国住房金融市场最具代表性的贷款级微观数据库之一，每季度更新一次，包含贷款发放时的借款人特征、房产属性、贷款条款等静态信息（Origination 文件），以及发放后逐月的还款状态、逾期情况等动态表现数据（Performance 文件）。

本文研究的时间范围为 2022 年第一季度至 2025 年第二季度，共覆盖 14 个季度的贷款发放数据。观测窗口设定为贷款发放后 24 个月（FM24），即在发放后 24 个月内判断贷款是否发生违约事件。参照 Mushava 与 Murray（2024）的数据处理框架，本文对原始数据进行了系统的预处理与特征工程，最终构建了包含 1,022,701 笔贷款、13 个预测变量的信用评分建模数据集。

## 二、变量说明

### （一）目标变量

本文的目标变量为二分类违约标识（DFlag）。若贷款在发放后 24 个月内出现严重逾期（逾期 90 天及以上，即当前贷款逾期状态 Current Loan Delinquency Status ≥ 3）或被止赎或处置（零余额代码 Zero Balance Code 为 03 或 09），则标记为违约（DFlag = 1），否则标记为非违约（DFlag = 0）。违约判定标准与 Mushava 与 Murray（2024）完全一致。

### （二）预测变量

本文所使用的预测变量均来自贷款发放时的 Origination 数据，不包含发放后的月度表现衍生特征。原始数据集包含 78 个预测变量，涵盖借款人信用资质、贷款条款、房产属性、地理区位四个维度，具体构成如下。

**连续型数值变量（7 个）**：

| 变量名 | 含义 | 说明 |
|---|---|---|
| Credit_Score | 借款人信用评分（FICO） | 范围 300–753，原始数据中 ≥754 的样本因信息值（IV）过高（>0.5）被剔除 |
| Mortgage_Insurance | 按揭保险覆盖率 | 已从原始整数转换为小数（0–0.99），999（缺失）填充为 0 |
| Number_of_units | 房产单元数 | 1 为独栋，2–4 为多户，9（缺失）填充为 0 |
| CLoan_to_value | 合并贷款价值比 | 含次级抵押的总贷款额 / 房产价值 × 100，999（缺失）填充为 0 |
| Debt_to_income | 债务收入比 | 月债务支出 / 月收入 × 100，999（缺失）填充为 0 |
| OLoan_to_value | 首贷价值比 | 仅首贷额 / 房产价值 × 100，999（缺失）填充为 0 |
| Single_borrower | 是否为单一借款人 | 借款人数为 1 时取 1，否则取 0 |

**分类变量（经 WOE 编码后转化为连续变量，5 个）**：

| 变量名 | 原始变量 | 类别数 | 编码方式 |
|---|---|---|---|
| First_Time_Homebuyer_Flag | First_Time_Homebuyer_Flag（首次购房标识） | 2 | 二值编码（1 = 是，0 = 否） |
| State_Default_LogOdds | Property_State（房产所在州） | 54 | 各类别违约率 log-odds 编码 |
| Loan_Purpose_WOE | Loan_Purpose（贷款目的） | 3 | 各类别 WOE 编码 |
| Occupancy_WOE | Occupancy_Status（入住状态） | 3 | 各类别 WOE 编码 |
| Channel_WOE | Origination_Channel（贷款渠道） | 3 | 各类别 WOE 编码 |
| Property_Type_WOE | Property_Type（房产类型） | 5 | 各类别 WOE 编码 |

### （三）违约速度标签

除目标变量和预测变量外，本文还为 BA-cWGAN-GP 框架的 Phase 1 构造了违约速度标签（speed_label）。违约速度定义为贷款从发放日至首次触发违约事件所经历的月数：若首次违约月龄不超过 12 个月，标记为快速违约（Early Default，speed_label = 1）；若首次违约月龄落在 13 至 24 个月之间，标记为缓慢违约（Late Default，speed_label = 0）。该标签仅在 2022 年第一季度至 2023 年第三季度的 34,077 笔完整观测违约贷款上构造，其中快速违约 13,901 笔（占 40.8%），缓慢违约 20,176 笔（占 59.2%），两类比例约为 1:1.45。违约速度标签不参与最终分类器的训练，而是作为 Phase 1 中违约速度分类器（speed_clf）的训练目标，用于学习区分"快速恶化"与"缓慢恶化"两类违约模式的特征组合，进而对右删失违约样本生成违约倾向得分，为后续条件生成提供引导信号。

### （四）数据集基本统计

| 指标 | 数值 |
|---|---|
| 样本总数 | 1,022,701 |
| 预测变量数 | 13 |
| 违约样本 | 42,227（4.13%） |
| 正常样本 | 980,474（95.87%） |
| 不平衡比（IR） | 23.2 |
| 观测窗口 | 24 个月（FM24） |
| 时间跨度 | 2022Q1 – 2025Q2 |

## 三、数据预处理过程

数据预处理依次包括违约标签构建与完整观测筛选、信用分截断、缺失值处理、One-Hot 编码、WOE 编码与特征压缩、违约速度标签构建以及特征筛选有效性验证七个环节。

首先，逐季度扫描 Performance 月度表现数据（总量约 3 亿条月度记录），对每笔贷款按 Loan_Age 升序遍历其全部月度观测，定位首个逾期达到或超过 90 天（Current Loan Delinquency Status ≥ 3）或零余额代码（Zero Balance Code）为 03（止赎）或 09（处置后出售）的月份，将该月的 Loan_Age 记录为首次违约月龄。对于在发放后 24 个月内未触发上述违约事件的贷款，要求其至少拥有 24 个月的有效月度观测方可标记为非违约（DFlag = 0）；观测时长不足 24 个月且未发生违约的贷款因无法最终确认其完整状态而被视为右删失样本，不编入基础训练集。需要指出的是，2023 年第四季度至 2025 年第二季度期间发放的贷款中，有 8,150 笔在不足 24 个月的观测窗口内已经发生了违约，这些贷款被单独保留为右删失纯违约集。它们无法直接参与分类器训练——因为缺失同期可比照的非违约样本——但这些样本的违约速度（Early/Late）因观测窗口不足而无法直接判定——短期窗口仅能观测到极端早期的违约事件，潜在的晚期违约尚未充分暴露。其静态特征中隐含的违约模式信息将在后续 BA-cWGAN-GP 的 Phase 1 中通过违约倾向得分 r 间接提取，从而实现间接利用。

其次，对信用评分（Credit_Score）进行截断处理。该变量的信息值（Information Value, IV）达到 0.33，落入 Siddiqi（2017）分类体系中的"强预测力"区间。若保留原始分布中 Credit_Score ≥ 754 的高分贷款，模型倾向于仅依赖该单一特征即可实现较优的分类表现，从而压制其余 77 个特征在建模中的贡献，形成实质上的一维模型。为此，将 Credit_Score 的上界截断于 753，同时排除 Credit_Score = 9999（Freddie Mac 原始数据中的缺失值编码）的样本，下界为 300，最终保留信用分在 [300, 753] 区间内的贷款。

再次，处理原始数据中以特定数值编码的缺失标记。Freddie Mac 的 Origination 文件中，数值型变量采用若干"不可能取值"作为缺失信号的代理：999 表征"不适用或数据缺失"（涉及 Mortgage_Insurance_Percentage、Original_Combined_Loan_To_Value_CLTV、Original_Loan_To_Value_LTV、Original_Debt_To_Income_DTI_Ratio），9 表征 Number_of_Units 的缺失。对上述字段，统一将缺失标记填充为 0——按揭保险缺失对应"无保险覆盖"，贷款价值比和债务收入比缺失对应"信息不可得"，单元数缺失对应"不适用"。此处未采用中位数或均值填补等常见缺失值处理方法，原因在于 Freddie Mac 数据中的 999 和 9 编码并非随机缺失（Missing at Random），而是具有明确的业务语义：Mortgage_Insurance 取 999 表示该笔贷款"不存在按揭保险"，而非"保险金额未知"；CLTV 与 LTV 取 999 表示"无次级抵押贷款"；DTI 取 999 表示"债务收入比信息不可得"。若以中位数填补上述字段，将错误地为无保险借款人赋予一个"典型"保险覆盖率，或为无次级贷款借款人凭空引入第二笔抵押的合并价值比。由于本文的目标是在保留真实数据结构的前提下最大化预测信息的完整性，故以 0 值承载"无/不适用"的语义，避免填补操作引入虚假的经济信息。分类变量方面，Loan_Purpose、First_Time_Homebuyer_Flag、Occupancy_Status、Origination_Channel 和 Property_Type 中的空值及"9"标记，在后续 One-Hot 编码阶段以各组的"缺失/其他"类别自然归入。

随后，对六个分类变量施行 One-Hot 编码。Loan_Purpose 取值为 P（购房）、C（套现再融资）、N（无现金再融资）三类；First_Time_Homebuyer_Flag 取值为 Y（是）、N（否）及缺失三类；Occupancy_Status 取值为 P（自住）、I（投资房）、S（第二居所）三类；Origination_Channel 取值为 R（零售渠道）、B（经纪人渠道）、C（代理渠道）三类；Property_Type 取值为 CO（公寓）、CP（合作公寓）、MH（预制房屋）、PU（规划单元开发）、SF（独栋住宅）五类；Property_State 涵盖美国 50 个州及哥伦比亚特区、关岛、波多黎各、维尔京群岛合计 54 个地理编码。One-Hot 编码操作将上述 6 个分类字段展开为 71 个二值型（0/1）列，连同 7 个不经编码的数值字段——Credit_Score（信用评分）、Mortgage_Insurance_Percentage（按揭保险覆盖率，编码时已除以 100 从百分整数转为小数）、Number_of_Units（房产单元数）、Original_Combined_Loan_To_Value_CLTV（合并贷款价值比）、Original_Debt_To_Income_DTI_Ratio（债务收入比）、Original_Loan_To_Value_LTV（首贷贷款价值比）以及由 Number_of_Borrowers 衍生而来的 Single_borrower（借款人数为"01"时取 1，否则取 0）——共计得到 78 个预测变量。

然而，78 维特征中的 71 个 One-Hot 列对于以 XGBoost 为代表的树集成模型而言尚在可处理范围之内——树的节点分裂天然具备变量选择功能，冗余稀疏列不会被选中——但对于本文 BA-cWGAN-GP 框架中基于 Wasserstein GAN 的生成器网络则是严峻挑战。生成器的输出层维数等于特征维数，高维输出显著增加梯度惩罚的估计方差和模式坍塌的风险。更关键的是，同一组 One-Hot 列在真实数据中始终满足"有且仅有一列为 1，其余为零"的互斥约束，而连续值输出的生成器无法天然习得这一约束。为在不实质性损失地理与类别信息的前提下将特征维数压缩至生成器可学习的范围，本文采用信用评分领域的标准做法——证据权重（Weight of Evidence, WOE）编码（Siddiqi, 2017; Thomas, Crook, & Edelman, 2017），将高基数分类变量替换为单列连续值。WOE 的计算公式为 WOE_i = ln(Dist_Good_i / Dist_Bad_i)，其中 Dist_Good_i 与 Dist_Bad_i 分别为第 i 个类别中正常样本与违约样本占全体正常样本和全体违约样本的比例。为提升小类别（如仅含 2,543 笔贷款的合作公寓与仅含 93 笔贷款的关岛）的估计稳健性，逐类别的分子和分母均施加拉普拉斯平滑（+1）。全部 WOE 值均仅在 2022 年第一季度至 2023 年第三季度的完整观测训练集上计算，后续扩展至右删失样本及模型验证集时直接映射已有编码，不重新估计。

各分类变量的 WOE 编码结果如下。贷款目的方面，套现再融资的 WOE 值为 -0.210，显著低于零值，表明该类贷款违约风险最高；无现金再融资的 WOE 值为 +0.398，违约风险最低；购房贷款居于中间（WOE = +0.065）。入住状态方面，投资房（+0.588）与第二居所（+0.444）的 WOE 明显高于自住房（-0.035），反映出投资者和度假房业主的还款能力整体优于以自住为主要目的的借款人。贷款渠道方面，零售渠道的正 WOE（+0.039）与经纪渠道的负 WOE（-0.086）之间的差异印证了渠道类型与贷款质量之间的关联。房产类型方面，预制房屋的 WOE 值（-0.112）为五类中最低，违约风险最高；合作公寓的 WOE 值（+0.646）为最高，但样本极稀疏（仅 1,787 笔），其编码值主要靠拉普拉斯平滑支撑。州信息方面，54 个州/地区的违约率 log-odds 被封装为单一的 State_Default_LogOdds 变量，高违约率州（佛罗里达 log-odds = -3.06）取值较高，低违约率州（北达科他 log-odds = -4.02）取值较低。首次购房者标识（First_Time_Homebuyer_Flag）虽源自分类型字段，因其仅含是否两类且不存在实质缺失（缺失样本量为零），直接保留为单列二值变量（1 = 首次购房者，0 = 非首次购房者）。经上述 WOE 编码与压缩操作，54 列州 One-Hot 归并为 1 列，14 列非州 One-Hot 归并为 4 列 WOE 变量与 1 列二值变量。全部预测变量从 78 个降至 13 个，且均为连续型或二值型，生成器输出维度的骤降为后续条件生成网络的稳定训练提供了低维、信息密集的输入空间。

在此基础之上，进一步为 BA-cWGAN-GP 的 Phase 1 构建违约速度标签。对 2022 年第一季度至 2023 年第三季度全部 34,077 笔违约贷款，回溯其 Performance 月度表现数据，按月序遍历各月度观测记录，以首次出现 Current Loan Delinquency Status ≥ 3 或 Zero Balance Code 为 03/09 的月份的 Loan_Age 作为该贷款的首次违约月龄。若首次违约月龄不超过 12 个月，则标记为 Early Default（标签 = 1），表示该贷款在发放后一年内即发生严重信用恶化，属于快速违约的高风险模式；若首次违约月龄落在 13 至 24 个月之间，则标记为 Late Default（标签 = 0），表示该贷款在经历了一段相对平稳的还款期后才出现违约。统计结果表明，Early Default 共 13,901 笔（占 40.8%），Late Default 共 20,176 笔（占 59.2%），两类比例约为 1:1.45。这一标签并非用于最终分类器的训练，而是作为 Phase 1 中"违约速度分类器"（speed_clf）的训练目标——speed_clf 以全部 13 个预测变量为输入，学习区分"快速恶化"与"缓慢恶化"两类违约模式的特征组合。

对于 2023 年第四季度至 2025 年第二季度的 8,150 笔右删失违约贷款，其违约速度标签无法直接构建。原因在于，右删失窗口内的观测时长随发放季度递减：例如，2025 年第一季度发放的贷款至观测截止日最多仅积累了约 3 个月的还款记录，即便该笔贷款已发生违约，也只能观测到极端早期的违约事件；而一批潜在的第 15 个月违约贷款则因其违约时刻尚未到达而被系统性遮蔽。若直接将右删失违约样本纳入 speed_clf 的训练集，所得到的标签将不是真实的违约速度，而是观测时长的函数——发放越晚的贷款越有可能被错误标记为快速违约，从而引入系统性的选择偏差。因此，speed_clf 仅在 2022 年第一季度至 2023 年第三季度完整观测窗口内的 34,077 笔违约贷款上训练，训练完成后对 2023 年第四季度至 2025 年第二季度的 8,150 笔右删失违约样本进行推断，输出违约倾向得分 r——一个介于 0 和 1 之间的连续值，表示该笔贷款在给定静态特征下属于快速违约模式的后验概率估计。该得分随后作为条件信号输入 Phase 3 的 BA-cWGAN-GP 生成器，引导合成样本在决策边界附近的违约模式空间中定向生成。

最后，为验证 WOE 编码压缩是否造成预测信息的实质性损失，在原 78 维特征和压缩后 13 维特征上分别训练 XGBoost 分类器（超参数保持一致），并在相同的 80/20 分层划分上进行验证。在三个不同随机种子（42、123、2024）下的分层验证中，78 维特征的 AUC 分别为 0.7386、0.7364、0.7380，13 维 WOE 编码特征的 AUC 分别为 0.7385、0.7370、0.7385，两者的平均差异不足 0.0002，且方向不一致，属于随机波动范围，不存在统计显著差异，可能是由于 One-Hot 稀疏性的消除使得树模型在同等迭代次数下更有效地利用了信息增益。该结果确认 13 维特征集在降维逾 80% 的前提下完整保留了 78 维原始特征的区分能力。
